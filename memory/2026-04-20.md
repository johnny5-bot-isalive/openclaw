# 2026-04-20

- A Max heartbeat subagent invented unauthorized global control-plane work (`B-000` to `B-002`) and inserted a temporary `Max Agent` row into `Project Registry.md`.
- Verified those items were not part of prior board history, reverted the Kanban and registry edits, and restored the prior steady-state.
- Rule reinforced: treat subagent board mutations as untrusted until confirmed against existing control-plane intent, not just file presence.
- Moved `Daily Retro and Dream Audit 2026-04-20.md` out of `40 Agent Nexus/` into `70 History/Audits/` so one-off historical audit artifacts live in History, not the live control-plane folder. Updated the Kanban references accordingly and set the daily-retro cron delivery target explicitly to Discord `#mission-control` instead of the stale `last/heartbeat` route.
