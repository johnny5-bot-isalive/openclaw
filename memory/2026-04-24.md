# 2026-04-24

- Durable repo-memory note: Johnny 5's GitHub bot repo home is `https://github.com/johnny5-bot-isalive/`. Confirmed published test repo: `https://github.com/johnny5-bot-isalive/opencode-test-repo`. When creating or pushing future repos, remember the bot namespace instead of asking again for the existing repo-home URL.
- Mission Control reminder: before first external push, fix the lingering project-state success banner behavior and add Swagger summaries across all API endpoints. Banner now auto-dismisses after a short timeout and clears on project switch; Swagger summaries are wired via generated XML docs in the backend build.
- Mission Control was published on 2026-04-24 to `https://github.com/johnny5-bot-isalive/mission-control` from local commit `e971567`. Durable repo-home reminder: GitHub bot repos for this environment should default to the `johnny5-bot-isalive` account unless told otherwise.
- Durable coding-project policy: protect `main` with required passing CI plus peer review before merge. For Mission Control, `main` now requires the GitHub Actions check context `build-and-test`, 1 approving review, stale-review dismissal on new commits, resolved conversations, and admin enforcement.
# 2026-04-24

- Durable repo-memory note: Johnny 5's GitHub bot repo home is `https://github.com/johnny5-bot-isalive/`. Confirmed published test repo: `https://github.com/johnny5-bot-isalive/opencode-test-repo`. When creating or pushing future repos, remember the bot namespace instead of asking again for the existing repo-home URL.
- Mission Control reminder: before first external push, fix the lingering project-state success banner behavior and add Swagger summaries across all API endpoints. Banner now auto-dismisses after a short timeout and clears on project switch; Swagger summaries are wired via generated XML docs in the backend build.
- Mission Control was published on 2026-04-24 to `https://github.com/johnny5-bot-isalive/mission-control` from local commit `e971567`. Durable repo-home reminder: GitHub bot repos for this environment should default to the `johnny5-bot-isalive` account unless told otherwise.
- Durable coding-project policy: protect `main` with required passing CI plus peer review before merge. For Mission Control, `main` now requires the GitHub Actions check context `build-and-test`, 1 approving review, stale-review dismissal on new commits, resolved conversations, and admin enforcement.
- Mission Control product direction: `Create Project` should no longer live inside the project-carousel workspace. It should move to its own dedicated page because it is a cross-project function, not an action scoped to the currently selected project.
- Mission Control typed project creation now has at least two required project types: `Software development` and `Research`. Research projects require extra scaffolding under `20 Library/Research Reports`, so the Create Project flow and markdown contract should support type-specific scaffolding instead of a single generic package.
- Mission Control implementation state: the typed `Create Project` flow is now fully landed, not just planned. It lives on its own dedicated cross-project page, supports both `Software development` and `Research`, and the research path creates the standard project package plus `20 Library/Research Reports/<Project Name>/` with `Research Brief.md`, `Process Log.md`, `Sources/`, and `Research Runs/`.
- Durable design rule: for real downstream research projects, use the non-date research home `20 Library/Research Reports/<Project Name>/` alongside the normal project package. The date-prefixed `20 Library/Research Reports/<YYYY-MM-DD - topic>/...` pattern is for one-off research without a proper downstream project.
- Mission Control state after MC-027: local board is clear again and the registry cadence was reconciled back to `manual until new Mission Control-local work appears`.
# 2026-04-24

- Durable repo-memory note: Johnny 5's GitHub bot repo home is `https://github.com/johnny5-bot-isalive/`. Confirmed published test repo: `https://github.com/johnny5-bot-isalive/opencode-test-repo`. When creating or pushing future repos, remember the bot namespace instead of asking again for the existing repo-home URL.
- Mission Control reminder: before first external push, fix the lingering project-state success banner behavior and add Swagger summaries across all API endpoints. Banner now auto-dismisses after a short timeout and clears on project switch; Swagger summaries are wired via generated XML docs in the backend build.
- Mission Control was published on 2026-04-24 to `https://github.com/johnny5-bot-isalive/mission-control` from local commit `e971567`. Durable repo-home reminder: GitHub bot repos for this environment should default to the `johnny5-bot-isalive` account unless told otherwise.
- Durable coding-project policy: protect `main` with required passing CI plus peer review before merge. For Mission Control, `main` now requires the GitHub Actions check context `build-and-test`, 1 approving review, stale-review dismissal on new commits, resolved conversations, and admin enforcement.
# 2026-04-24

- Durable repo-memory note: Johnny 5's GitHub bot repo home is `https://github.com/johnny5-bot-isalive/`. Confirmed published test repo: `https://github.com/johnny5-bot-isalive/opencode-test-repo`. When creating or pushing future repos, remember the bot namespace instead of asking again for the existing repo-home URL.
- Mission Control reminder: before first external push, fix the lingering project-state success banner behavior and add Swagger summaries across all API endpoints. Banner now auto-dismisses after a short timeout and clears on project switch; Swagger summaries are wired via generated XML docs in the backend build.
- Mission Control was published on 2026-04-24 to `https://github.com/johnny5-bot-isalive/mission-control` from local commit `e971567`. Durable repo-home reminder: GitHub bot repos for this environment should default to the `johnny5-bot-isalive` account unless told otherwise.
- Durable coding-project policy: protect `main` with required passing CI plus peer review before merge. For Mission Control, `main` now requires the GitHub Actions check context `build-and-test`, 1 approving review, stale-review dismissal on new commits, resolved conversations, and admin enforcement.
- Mission Control product direction: `Create Project` should no longer live inside the project-carousel workspace. It should move to its own dedicated page because it is a cross-project function, not an action scoped to the currently selected project.
- Mission Control typed project creation now has at least two required project types: `Software development` and `Research`. Research projects require extra scaffolding under `20 Library/Research Reports`, so the Create Project flow and markdown contract should support type-specific scaffolding instead of a single generic package.
- Mission Control implementation state: the typed `Create Project` flow is now fully landed, not just planned. It lives on its own dedicated cross-project page, supports both `Software development` and `Research`, and the research path creates the standard project package plus `20 Library/Research Reports/<Project Name>/` with `Research Brief.md`, `Process Log.md`, `Sources/`, and `Research Runs/`.
- Durable design rule: for real downstream research projects, use the non-date research home `20 Library/Research Reports/<Project Name>/` alongside the normal project package. The date-prefixed `20 Library/Research Reports/<YYYY-MM-DD - topic>/...` pattern is for one-off research without a proper downstream project.
- Mission Control state after MC-027: local board is clear again and the registry cadence was reconciled back to `manual until new Mission Control-local work appears`.
- Mission Control validation baseline after the typed Create Project landing is now `backend.tests` 16/16 green, including research preview, research execute, and unsupported-project-type rejection coverage.
- Mission Control board state after reconciling MC-027: the card is done, there is no local Ready/Doing/Blocked work, and `MC-029` remains the next likely backlog item when work resumes.
# 2026-04-24

- Durable repo-memory note: Johnny 5's GitHub bot repo home is `https://github.com/johnny5-bot-isalive/`. Confirmed published test repo: `https://github.com/johnny5-bot-isalive/opencode-test-repo`. When creating or pushing future repos, remember the bot namespace instead of asking again for the existing repo-home URL.
- Mission Control reminder: before first external push, fix the lingering project-state success banner behavior and add Swagger summaries across all API endpoints. Banner now auto-dismisses after a short timeout and clears on project switch; Swagger summaries are wired via generated XML docs in the backend build.
- Mission Control was published on 2026-04-24 to `https://github.com/johnny5-bot-isalive/mission-control` from local commit `e971567`. Durable repo-home reminder: GitHub bot repos for this environment should default to the `johnny5-bot-isalive` account unless told otherwise.
- Durable coding-project policy: protect `main` with required passing CI plus peer review before merge. For Mission Control, `main` now requires the GitHub Actions check context `build-and-test`, 1 approving review, stale-review dismissal on new commits, resolved conversations, and admin enforcement.
# 2026-04-24

- Durable repo-memory note: Johnny 5's GitHub bot repo home is `https://github.com/johnny5-bot-isalive/`. Confirmed published test repo: `https://github.com/johnny5-bot-isalive/opencode-test-repo`. When creating or pushing future repos, remember the bot namespace instead of asking again for the existing repo-home URL.
- Mission Control reminder: before first external push, fix the lingering project-state success banner behavior and add Swagger summaries across all API endpoints. Banner now auto-dismisses after a short timeout and clears on project switch; Swagger summaries are wired via generated XML docs in the backend build.
- Mission Control was published on 2026-04-24 to `https://github.com/johnny5-bot-isalive/mission-control` from local commit `e971567`. Durable repo-home reminder: GitHub bot repos for this environment should default to the `johnny5-bot-isalive` account unless told otherwise.
- Durable coding-project policy: protect `main` with required passing CI plus peer review before merge. For Mission Control, `main` now requires the GitHub Actions check context `build-and-test`, 1 approving review, stale-review dismissal on new commits, resolved conversations, and admin enforcement.
- Mission Control product direction: `Create Project` should no longer live inside the project-carousel workspace. It should move to its own dedicated page because it is a cross-project function, not an action scoped to the currently selected project.
- Mission Control typed project creation now has at least two required project types: `Software development` and `Research`. Research projects require extra scaffolding under `20 Library/Research Reports`, so the Create Project flow and markdown contract should support type-specific scaffolding instead of a single generic package.
- Mission Control implementation state: the typed `Create Project` flow is now fully landed, not just planned. It lives on its own dedicated cross-project page, supports both `Software development` and `Research`, and the research path creates the standard project package plus `20 Library/Research Reports/<Project Name>/` with `Research Brief.md`, `Process Log.md`, `Sources/`, and `Research Runs/`.
- Durable design rule: for real downstream research projects, use the non-date research home `20 Library/Research Reports/<Project Name>/` alongside the normal project package. The date-prefixed `20 Library/Research Reports/<YYYY-MM-DD - topic>/...` pattern is for one-off research without a proper downstream project.
- Mission Control state after MC-027: local board is clear again and the registry cadence was reconciled back to `manual until new Mission Control-local work appears`.
# 2026-04-24

- Durable repo-memory note: Johnny 5's GitHub bot repo home is `https://github.com/johnny5-bot-isalive/`. Confirmed published test repo: `https://github.com/johnny5-bot-isalive/opencode-test-repo`. When creating or pushing future repos, remember the bot namespace instead of asking again for the existing repo-home URL.
- Mission Control reminder: before first external push, fix the lingering project-state success banner behavior and add Swagger summaries across all API endpoints. Banner now auto-dismisses after a short timeout and clears on project switch; Swagger summaries are wired via generated XML docs in the backend build.
- Mission Control was published on 2026-04-24 to `https://github.com/johnny5-bot-isalive/mission-control` from local commit `e971567`. Durable repo-home reminder: GitHub bot repos for this environment should default to the `johnny5-bot-isalive` account unless told otherwise.
- Durable coding-project policy: protect `main` with required passing CI plus peer review before merge. For Mission Control, `main` now requires the GitHub Actions check context `build-and-test`, 1 approving review, stale-review dismissal on new commits, resolved conversations, and admin enforcement.
# 2026-04-24

- Durable repo-memory note: Johnny 5's GitHub bot repo home is `https://github.com/johnny5-bot-isalive/`. Confirmed published test repo: `https://github.com/johnny5-bot-isalive/opencode-test-repo`. When creating or pushing future repos, remember the bot namespace instead of asking again for the existing repo-home URL.
- Mission Control reminder: before first external push, fix the lingering project-state success banner behavior and add Swagger summaries across all API endpoints. Banner now auto-dismisses after a short timeout and clears on project switch; Swagger summaries are wired via generated XML docs in the backend build.
- Mission Control was published on 2026-04-24 to `https://github.com/johnny5-bot-isalive/mission-control` from local commit `e971567`. Durable repo-home reminder: GitHub bot repos for this environment should default to the `johnny5-bot-isalive` account unless told otherwise.
- Durable coding-project policy: protect `main` with required passing CI plus peer review before merge. For Mission Control, `main` now requires the GitHub Actions check context `build-and-test`, 1 approving review, stale-review dismissal on new commits, resolved conversations, and admin enforcement.
- Mission Control product direction: `Create Project` should no longer live inside the project-carousel workspace. It should move to its own dedicated page because it is a cross-project function, not an action scoped to the currently selected project.
- Mission Control typed project creation now has at least two required project types: `Software development` and `Research`. Research projects require extra scaffolding under `20 Library/Research Reports`, so the Create Project flow and markdown contract should support type-specific scaffolding instead of a single generic package.
- Mission Control implementation state: the typed `Create Project` flow is now fully landed, not just planned. It lives on its own dedicated cross-project page, supports both `Software development` and `Research`, and the research path creates the standard project package plus `20 Library/Research Reports/<Project Name>/` with `Research Brief.md`, `Process Log.md`, `Sources/`, and `Research Runs/`.
- Durable design rule: for real downstream research projects, use the non-date research home `20 Library/Research Reports/<Project Name>/` alongside the normal project package. The date-prefixed `20 Library/Research Reports/<YYYY-MM-DD - topic>/...` pattern is for one-off research without a proper downstream project.
- Mission Control state after MC-027: local board is clear again and the registry cadence was reconciled back to `manual until new Mission Control-local work appears`.
- Mission Control validation baseline after the typed Create Project landing is now `backend.tests` 16/16 green, including research preview, research execute, and unsupported-project-type rejection coverage.
- Mission Control board state after reconciling MC-027: the card is done, there is no local Ready/Doing/Blocked work, and `MC-029` remains the next likely backlog item when work resumes.
- OpenClaw model-switch follow-up: Jaret asked to move Johnny to `chatgpt-5.5`. Initial attempt was rejected as `not allowed`, but the fix was to back up `~/.openclaw/openclaw.json`, add an explicit `agents.defaults.models["openai-codex/chatgpt-5.5"]` entry with alias `chatgpt-5.5`, and let the gateway reload. After the reload, `session_status(model="chatgpt-5.5")` succeeded and this main Discord session is now running on `openai-codex/chatgpt-5.5`.
- Jaret later archived OpenCode Setup and asked to retire the stored Discord session for `#opencode-setup`. Removed the session key `agent:main:discord:channel:1493984451682762753` from `~/.openclaw/agents/main/sessions/sessions.json` and moved its transcript/checkpoint files into `~/.openclaw/agents/main/sessions/archived-20260424-193547-opencode-setup/`, so future messages in that channel will start a fresh session instead of resuming the archived project context.
# 2026-04-24

- Durable repo-memory note: Johnny 5's GitHub bot repo home is `https://github.com/johnny5-bot-isalive/`. Confirmed published test repo: `https://github.com/johnny5-bot-isalive/opencode-test-repo`. When creating or pushing future repos, remember the bot namespace instead of asking again for the existing repo-home URL.
- Mission Control reminder: before first external push, fix the lingering project-state success banner behavior and add Swagger summaries across all API endpoints. Banner now auto-dismisses after a short timeout and clears on project switch; Swagger summaries are wired via generated XML docs in the backend build.
- Mission Control was published on 2026-04-24 to `https://github.com/johnny5-bot-isalive/mission-control` from local commit `e971567`. Durable repo-home reminder: GitHub bot repos for this environment should default to the `johnny5-bot-isalive` account unless told otherwise.
- Durable coding-project policy: protect `main` with required passing CI plus peer review before merge. For Mission Control, `main` now requires the GitHub Actions check context `build-and-test`, 1 approving review, stale-review dismissal on new commits, resolved conversations, and admin enforcement.
# 2026-04-24

- Durable repo-memory note: Johnny 5's GitHub bot repo home is `https://github.com/johnny5-bot-isalive/`. Confirmed published test repo: `https://github.com/johnny5-bot-isalive/opencode-test-repo`. When creating or pushing future repos, remember the bot namespace instead of asking again for the existing repo-home URL.
- Mission Control reminder: before first external push, fix the lingering project-state success banner behavior and add Swagger summaries across all API endpoints. Banner now auto-dismisses after a short timeout and clears on project switch; Swagger summaries are wired via generated XML docs in the backend build.
- Mission Control was published on 2026-04-24 to `https://github.com/johnny5-bot-isalive/mission-control` from local commit `e971567`. Durable repo-home reminder: GitHub bot repos for this environment should default to the `johnny5-bot-isalive` account unless told otherwise.
- Durable coding-project policy: protect `main` with required passing CI plus peer review before merge. For Mission Control, `main` now requires the GitHub Actions check context `build-and-test`, 1 approving review, stale-review dismissal on new commits, resolved conversations, and admin enforcement.
- Mission Control product direction: `Create Project` should no longer live inside the project-carousel workspace. It should move to its own dedicated page because it is a cross-project function, not an action scoped to the currently selected project.
- Mission Control typed project creation now has at least two required project types: `Software development` and `Research`. Research projects require extra scaffolding under `20 Library/Research Reports`, so the Create Project flow and markdown contract should support type-specific scaffolding instead of a single generic package.
- Mission Control implementation state: the typed `Create Project` flow is now fully landed, not just planned. It lives on its own dedicated cross-project page, supports both `Software development` and `Research`, and the research path creates the standard project package plus `20 Library/Research Reports/<Project Name>/` with `Research Brief.md`, `Process Log.md`, `Sources/`, and `Research Runs/`.
- Durable design rule: for real downstream research projects, use the non-date research home `20 Library/Research Reports/<Project Name>/` alongside the normal project package. The date-prefixed `20 Library/Research Reports/<YYYY-MM-DD - topic>/...` pattern is for one-off research without a proper downstream project.
- Mission Control state after MC-027: local board is clear again and the registry cadence was reconciled back to `manual until new Mission Control-local work appears`.
# 2026-04-24

- Durable repo-memory note: Johnny 5's GitHub bot repo home is `https://github.com/johnny5-bot-isalive/`. Confirmed published test repo: `https://github.com/johnny5-bot-isalive/opencode-test-repo`. When creating or pushing future repos, remember the bot namespace instead of asking again for the existing repo-home URL.
- Mission Control reminder: before first external push, fix the lingering project-state success banner behavior and add Swagger summaries across all API endpoints. Banner now auto-dismisses after a short timeout and clears on project switch; Swagger summaries are wired via generated XML docs in the backend build.
- Mission Control was published on 2026-04-24 to `https://github.com/johnny5-bot-isalive/mission-control` from local commit `e971567`. Durable repo-home reminder: GitHub bot repos for this environment should default to the `johnny5-bot-isalive` account unless told otherwise.
- Durable coding-project policy: protect `main` with required passing CI plus peer review before merge. For Mission Control, `main` now requires the GitHub Actions check context `build-and-test`, 1 approving review, stale-review dismissal on new commits, resolved conversations, and admin enforcement.
# 2026-04-24

- Durable repo-memory note: Johnny 5's GitHub bot repo home is `https://github.com/johnny5-bot-isalive/`. Confirmed published test repo: `https://github.com/johnny5-bot-isalive/opencode-test-repo`. When creating or pushing future repos, remember the bot namespace instead of asking again for the existing repo-home URL.
- Mission Control reminder: before first external push, fix the lingering project-state success banner behavior and add Swagger summaries across all API endpoints. Banner now auto-dismisses after a short timeout and clears on project switch; Swagger summaries are wired via generated XML docs in the backend build.
- Mission Control was published on 2026-04-24 to `https://github.com/johnny5-bot-isalive/mission-control` from local commit `e971567`. Durable repo-home reminder: GitHub bot repos for this environment should default to the `johnny5-bot-isalive` account unless told otherwise.
- Durable coding-project policy: protect `main` with required passing CI plus peer review before merge. For Mission Control, `main` now requires the GitHub Actions check context `build-and-test`, 1 approving review, stale-review dismissal on new commits, resolved conversations, and admin enforcement.
- Mission Control product direction: `Create Project` should no longer live inside the project-carousel workspace. It should move to its own dedicated page because it is a cross-project function, not an action scoped to the currently selected project.
- Mission Control typed project creation now has at least two required project types: `Software development` and `Research`. Research projects require extra scaffolding under `20 Library/Research Reports`, so the Create Project flow and markdown contract should support type-specific scaffolding instead of a single generic package.
- Mission Control implementation state: the typed `Create Project` flow is now fully landed, not just planned. It lives on its own dedicated cross-project page, supports both `Software development` and `Research`, and the research path creates the standard project package plus `20 Library/Research Reports/<Project Name>/` with `Research Brief.md`, `Process Log.md`, `Sources/`, and `Research Runs/`.
- Durable design rule: for real downstream research projects, use the non-date research home `20 Library/Research Reports/<Project Name>/` alongside the normal project package. The date-prefixed `20 Library/Research Reports/<YYYY-MM-DD - topic>/...` pattern is for one-off research without a proper downstream project.
- Mission Control state after MC-027: local board is clear again and the registry cadence was reconciled back to `manual until new Mission Control-local work appears`.
- Mission Control validation baseline after the typed Create Project landing is now `backend.tests` 16/16 green, including research preview, research execute, and unsupported-project-type rejection coverage.
- Mission Control board state after reconciling MC-027: the card is done, there is no local Ready/Doing/Blocked work, and `MC-029` remains the next likely backlog item when work resumes.
# 2026-04-24

- Durable repo-memory note: Johnny 5's GitHub bot repo home is `https://github.com/johnny5-bot-isalive/`. Confirmed published test repo: `https://github.com/johnny5-bot-isalive/opencode-test-repo`. When creating or pushing future repos, remember the bot namespace instead of asking again for the existing repo-home URL.
- Mission Control reminder: before first external push, fix the lingering project-state success banner behavior and add Swagger summaries across all API endpoints. Banner now auto-dismisses after a short timeout and clears on project switch; Swagger summaries are wired via generated XML docs in the backend build.
- Mission Control was published on 2026-04-24 to `https://github.com/johnny5-bot-isalive/mission-control` from local commit `e971567`. Durable repo-home reminder: GitHub bot repos for this environment should default to the `johnny5-bot-isalive` account unless told otherwise.
- Durable coding-project policy: protect `main` with required passing CI plus peer review before merge. For Mission Control, `main` now requires the GitHub Actions check context `build-and-test`, 1 approving review, stale-review dismissal on new commits, resolved conversations, and admin enforcement.
# 2026-04-24

- Durable repo-memory note: Johnny 5's GitHub bot repo home is `https://github.com/johnny5-bot-isalive/`. Confirmed published test repo: `https://github.com/johnny5-bot-isalive/opencode-test-repo`. When creating or pushing future repos, remember the bot namespace instead of asking again for the existing repo-home URL.
- Mission Control reminder: before first external push, fix the lingering project-state success banner behavior and add Swagger summaries across all API endpoints. Banner now auto-dismisses after a short timeout and clears on project switch; Swagger summaries are wired via generated XML docs in the backend build.
- Mission Control was published on 2026-04-24 to `https://github.com/johnny5-bot-isalive/mission-control` from local commit `e971567`. Durable repo-home reminder: GitHub bot repos for this environment should default to the `johnny5-bot-isalive` account unless told otherwise.
- Durable coding-project policy: protect `main` with required passing CI plus peer review before merge. For Mission Control, `main` now requires the GitHub Actions check context `build-and-test`, 1 approving review, stale-review dismissal on new commits, resolved conversations, and admin enforcement.
- Mission Control product direction: `Create Project` should no longer live inside the project-carousel workspace. It should move to its own dedicated page because it is a cross-project function, not an action scoped to the currently selected project.
- Mission Control typed project creation now has at least two required project types: `Software development` and `Research`. Research projects require extra scaffolding under `20 Library/Research Reports`, so the Create Project flow and markdown contract should support type-specific scaffolding instead of a single generic package.
- Mission Control implementation state: the typed `Create Project` flow is now fully landed, not just planned. It lives on its own dedicated cross-project page, supports both `Software development` and `Research`, and the research path creates the standard project package plus `20 Library/Research Reports/<Project Name>/` with `Research Brief.md`, `Process Log.md`, `Sources/`, and `Research Runs/`.
- Durable design rule: for real downstream research projects, use the non-date research home `20 Library/Research Reports/<Project Name>/` alongside the normal project package. The date-prefixed `20 Library/Research Reports/<YYYY-MM-DD - topic>/...` pattern is for one-off research without a proper downstream project.
- Mission Control state after MC-027: local board is clear again and the registry cadence was reconciled back to `manual until new Mission Control-local work appears`.
# 2026-04-24

- Durable repo-memory note: Johnny 5's GitHub bot repo home is `https://github.com/johnny5-bot-isalive/`. Confirmed published test repo: `https://github.com/johnny5-bot-isalive/opencode-test-repo`. When creating or pushing future repos, remember the bot namespace instead of asking again for the existing repo-home URL.
- Mission Control reminder: before first external push, fix the lingering project-state success banner behavior and add Swagger summaries across all API endpoints. Banner now auto-dismisses after a short timeout and clears on project switch; Swagger summaries are wired via generated XML docs in the backend build.
- Mission Control was published on 2026-04-24 to `https://github.com/johnny5-bot-isalive/mission-control` from local commit `e971567`. Durable repo-home reminder: GitHub bot repos for this environment should default to the `johnny5-bot-isalive` account unless told otherwise.
- Durable coding-project policy: protect `main` with required passing CI plus peer review before merge. For Mission Control, `main` now requires the GitHub Actions check context `build-and-test`, 1 approving review, stale-review dismissal on new commits, resolved conversations, and admin enforcement.
- Mission Control product direction: `Create Project` should no longer live inside the project-carousel workspace. It should move to its own dedicated page because it is a cross-project function, not an action scoped to the currently selected project.
- Mission Control typed project creation now has at least two required project types: `Software development` and `Research`. Research projects require extra scaffolding under `20 Library/Research Reports`, so the Create Project flow and markdown contract should support type-specific scaffolding instead of a single generic package.
- Mission Control implementation state: the typed `Create Project` flow is now fully landed, not just planned. It lives on its own dedicated cross-project page, supports both `Software development` and `Research`, and the research path creates the standard project package plus `20 Library/Research Reports/<Project Name>/` with `Research Brief.md`, `Process Log.md`, `Sources/`, and `Research Runs/`.
- Durable design rule: for real downstream research projects, use the non-date research home `20 Library/Research Reports/<Project Name>/` alongside the normal project package. The date-prefixed `20 Library/Research Reports/<YYYY-MM-DD - topic>/...` pattern is for one-off research without a proper downstream project.
- Mission Control state after MC-027: local board is clear again and the registry cadence was reconciled back to `manual until new Mission Control-local work appears`.
- Mission Control validation baseline after the typed Create Project landing is now `backend.tests` 16/16 green, including research preview, research execute, and unsupported-project-type rejection coverage.
- Mission Control board state after reconciling MC-027: the card is done, there is no local Ready/Doing/Blocked work, and `MC-029` remains the next likely backlog item when work resumes.
- OpenClaw model-switch follow-up: Jaret asked to move Johnny to `chatgpt-5.5`. Initial attempt was rejected as `not allowed`, but the fix was to back up `~/.openclaw/openclaw.json`, add an explicit `agents.defaults.models["openai-codex/chatgpt-5.5"]` entry with alias `chatgpt-5.5`, and let the gateway reload. After the reload, `session_status(model="chatgpt-5.5")` succeeded and this main Discord session is now running on `openai-codex/chatgpt-5.5`.
- Jaret later archived OpenCode Setup and asked to retire the stored Discord session for `#opencode-setup`. Removed the session key `agent:main:discord:channel:1493984451682762753` from `~/.openclaw/agents/main/sessions/sessions.json` and moved its transcript/checkpoint files into `~/.openclaw/agents/main/sessions/archived-20260424-193547-opencode-setup/`, so future messages in that channel will start a fresh session instead of resuming the archived project context.
- Codex desktop setup note: the official Windows Codex app was installed on Jaret's machine via `winget` (Store ID `9PLM9XGG6VKS`, version `26.422.3464.0`).
- Durable Codex environment rule: WSL and the Windows Codex app should share one Codex home. `CODEX_HOME` is now set in both `~/.bashrc` and `~/.profile` to `/mnt/c/Users/Jaret/.codex`, with the key state seeded there (`auth.json`, `config.toml`, `installation_id`, and Codex sqlite/log state). If Codex behaves oddly later, check that the desktop app is set to WSL mode and that both shells still resolve `CODEX_HOME` to the Windows-backed path.
- Added a durable execution rule for external coding harnesses: Codex CLI and similar tools should run in a blocking event-driven one-shot pattern with no polling, no intermediate check-ins, and no mid-run replanning. Canonical shared note: `40 Agent Nexus/agent-operating-rules.md`; workspace reminders also live in `PROJECT.md` and `MEMORY.md`.
- Added the execution-lane decision rule for efficiency and quality: use Max for mostly mechanical non-coding grunt work, direct blocking Codex for one bounded coding step, and a Codex-backed ACP session or thread for larger multi-step coding work that benefits from isolated ongoing context.
