# REM Sleep

### Reflections
- No strong patterns surfaced.

### Possible Lasting Truths
- - An implementation checklist was then created in Obsidian Inbox at `00 Inbox/OpenCode Setup Implementation Checklist.md`. The first live setup step was completed by adding an explicit `acp` block to `~/.openclaw/openclaw.json` with `enabled: true`, `backend: "acpx"`, `defaultAgent: "opencode"`, and `allowedAgents: ["opencode"]`; the next required step is a gateway restart before running an OpenCode ACP smoke test. - Jaret later moved the OpenCode planning notes into `40 Agent Nexus/Projects/OpenCode Setup/`. After the gateway restart, the remaining OpenCode implementation work was converted into heartbeat-ready backlog items G-028 through G-032, with grunt-work phases assigned to Max and th [confidence=0.78 evidence=memory/2026-04-13.md:44-47]
- # 2026-04-15 - Diagnosed the missed 9:00 AM OpenClaw update reminder: the daily update-check cron did run, but it was configured as an internal `systemEvent` path without explicit announce/delivery. Live cron status showed `lastDeliveryStatus: "not-requested"`, so no user-facing prompt was sent. - Practical implication: the update reminder currently depends on implicit session routing and is not reliable. It should be patched to explicit delivery to a chosen Discord channel when an update is available. - Fixed the two cron issues later that day using native isolated cron jobs instead of fragile main-session system events: the 9:00 AM updater now runs `openclaw update status` and announces i [confidence=0.78 evidence=memory/2026-04-15.md:1-7]
- # 2026-04-19 - After RI-039 passed, Jaret approved a tiny cleanup pass instead of reopening RI. The two low-risk fixes were applied: the generated run summary path was corrected from `./Process Log.md` to `../Process Log.md`, and `00 Inbox/Research Process Retest Assertions - 2026-04-18.md` was updated so the expected process-log location matches the canonical top-level `Process Log.md` pattern. - Jaret then clarified that deep research should outperform a plain one-shot ChatGPT research pass by using self-improving search-pattern refinement as the default Mode 3 behavior, not only as a gap-filling exception. The shared research method was updated accordingly: Mode 3 now defaults to iterati [confidence=0.78 evidence=memory/2026-04-19.md:1-7]
