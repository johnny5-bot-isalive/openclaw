# 2026-04-19

- After RI-039 passed, Jaret approved a tiny cleanup pass instead of reopening RI. The two low-risk fixes were applied: the generated run summary path was corrected from `./Process Log.md` to `../Process Log.md`, and `00 Inbox/Research Process Retest Assertions - 2026-04-18.md` was updated so the expected process-log location matches the canonical top-level `Process Log.md` pattern.
- Jaret then clarified that deep research should outperform a plain one-shot ChatGPT research pass by using self-improving search-pattern refinement as the default Mode 3 behavior, not only as a gap-filling exception. The shared research method was updated accordingly: Mode 3 now defaults to iterative refinement, the recursion note explicitly endorses taxonomy-split refinement like the red-light-helmet example, and the default bounded cap is up to 5 total cycles when another pass is still clearly earning its keep.
- Jaret then asked for a consolidation pass instead of more note sprawl. The shared research package was tightened by folding the old readiness-gates and starter-kit docs into `Research-Enabled Project Contract.md`, folding the review-workflow doc into `Research Run Review Template.md`, updating startup surfaces to load the smaller set, and adding ongoing-research continuity fields to the surviving brief, run-summary, and dossier templates.
# 2026-04-19

- After RI-039 passed, Jaret approved a tiny cleanup pass instead of reopening RI. The two low-risk fixes were applied: the generated run summary path was corrected from `./Process Log.md` to `../Process Log.md`, and `00 Inbox/Research Process Retest Assertions - 2026-04-18.md` was updated so the expected process-log location matches the canonical top-level `Process Log.md` pattern.
- Jaret then clarified that deep research should outperform a plain one-shot ChatGPT research pass by using self-improving search-pattern refinement as the default Mode 3 behavior, not only as a gap-filling exception. The shared research method was updated accordingly: Mode 3 now defaults to iterative refinement, the recursion note explicitly endorses taxonomy-split refinement like the red-light-helmet example, and the default bounded cap is up to 5 total cycles when another pass is still clearly earning its keep.
- Jaret then asked for a consolidation pass instead of more note sprawl. The shared research package was tightened by folding the old readiness-gates and starter-kit docs into `Research-Enabled Project Contract.md`, folding the review-workflow doc into `Research Run Review Template.md`, updating startup surfaces to load the smaller set, and adding ongoing-research continuity fields to the surviving brief, run-summary, and dossier templates.
- The Mode 3 robot-mower research project in `00 Inbox/Research Intake/2026-04-19 - robot mower lidar 2026/` was advanced through run-003. The key correction was that **Roborock RockMow Z120 LiDAR is a real model/SKU branch** and maps to the consumer-facing **Z1 LiDAR** family, with **X1 LiDAR** treated as the later US-facing continuation rather than a separately scored mower.
- Current corrected ranking for Jaret’s Duvall-property rubric is: **1) Mammotion LUBA 3 AWD (85.3), 2) Roborock RockMow Z120/Z1 LiDAR (84.4), 3) Dreame A3 AWD Pro 3500 (83.5), 4) Segway Navimow i2 LiDAR Pro (80.5), 5) ECOVACS GOAT A3000 LiDAR PRO (77.3), 6) MOVA LiDAX Ultra 3000 AWD (77.2)**. Bottom-line recommendation remains LUBA 3 for the exact yard, with Roborock now the main corrected #2 contender.
# 2026-04-19

- After RI-039 passed, Jaret approved a tiny cleanup pass instead of reopening RI. The two low-risk fixes were applied: the generated run summary path was corrected from `./Process Log.md` to `../Process Log.md`, and `00 Inbox/Research Process Retest Assertions - 2026-04-18.md` was updated so the expected process-log location matches the canonical top-level `Process Log.md` pattern.
- Jaret then clarified that deep research should outperform a plain one-shot ChatGPT research pass by using self-improving search-pattern refinement as the default Mode 3 behavior, not only as a gap-filling exception. The shared research method was updated accordingly: Mode 3 now defaults to iterative refinement, the recursion note explicitly endorses taxonomy-split refinement like the red-light-helmet example, and the default bounded cap is up to 5 total cycles when another pass is still clearly earning its keep.
- Jaret then asked for a consolidation pass instead of more note sprawl. The shared research package was tightened by folding the old readiness-gates and starter-kit docs into `Research-Enabled Project Contract.md`, folding the review-workflow doc into `Research Run Review Template.md`, updating startup surfaces to load the smaller set, and adding ongoing-research continuity fields to the surviving brief, run-summary, and dossier templates.
# 2026-04-19

- After RI-039 passed, Jaret approved a tiny cleanup pass instead of reopening RI. The two low-risk fixes were applied: the generated run summary path was corrected from `./Process Log.md` to `../Process Log.md`, and `00 Inbox/Research Process Retest Assertions - 2026-04-18.md` was updated so the expected process-log location matches the canonical top-level `Process Log.md` pattern.
- Jaret then clarified that deep research should outperform a plain one-shot ChatGPT research pass by using self-improving search-pattern refinement as the default Mode 3 behavior, not only as a gap-filling exception. The shared research method was updated accordingly: Mode 3 now defaults to iterative refinement, the recursion note explicitly endorses taxonomy-split refinement like the red-light-helmet example, and the default bounded cap is up to 5 total cycles when another pass is still clearly earning its keep.
- Jaret then asked for a consolidation pass instead of more note sprawl. The shared research package was tightened by folding the old readiness-gates and starter-kit docs into `Research-Enabled Project Contract.md`, folding the review-workflow doc into `Research Run Review Template.md`, updating startup surfaces to load the smaller set, and adding ongoing-research continuity fields to the surviving brief, run-summary, and dossier templates.
- The Mode 3 robot-mower research project in `00 Inbox/Research Intake/2026-04-19 - robot mower lidar 2026/` was advanced through run-003. The key correction was that **Roborock RockMow Z120 LiDAR is a real model/SKU branch** and maps to the consumer-facing **Z1 LiDAR** family, with **X1 LiDAR** treated as the later US-facing continuation rather than a separately scored mower.
- Current corrected ranking for Jaret’s Duvall-property rubric is: **1) Mammotion LUBA 3 AWD (85.3), 2) Roborock RockMow Z120/Z1 LiDAR (84.4), 3) Dreame A3 AWD Pro 3500 (83.5), 4) Segway Navimow i2 LiDAR Pro (80.5), 5) ECOVACS GOAT A3000 LiDAR PRO (77.3), 6) MOVA LiDAX Ultra 3000 AWD (77.2)**. Bottom-line recommendation remains LUBA 3 for the exact yard, with Roborock now the main corrected #2 contender.
- Jaret clarified a durable workflow preference: `00 Inbox` is appropriate for temporary intake, tests, or notes that need review/decision, but real research reports should live in `Library` rather than remaining in the inbox.
- Reviewing the robot-mower process-compliance rerun after the added second pass, the research workflow itself now effectively passes: Mode 3 was explicit, preflight artifacts came before external research, local-first grounding was documented, source captures were separate, and iterative run artifacts existed. The remaining strict assertion gap was the final handoff reply, which named artifacts but did not explicitly restate the chosen mode and rationale.
- The heartbeat-reported QMD failure on semantic query `robot mower lidar AWD edge mowing` did not reproduce locally. A direct CPU-only rerun succeeded in about 4.5 seconds with roughly 571 MB RSS, so the best current read is intermittent external `SIGKILL` or runtime/supervisor resource pressure rather than a deterministic QMD query bug. Operational takeaway: prefer `qmd search` first in heartbeat-style retrieval and escalate to `qmd query` only when semantic reranking is actually needed.
- Jaret ratified a durable storage split for research outputs: real research report packages should default to `20 Library/Research Reports/<YYYY-MM-DD - topic>/`, while `00 Inbox/Research Intake/` should be reserved for temporary process tests, temporary intake material, or explicit inbox-review routing. The shared research startup rules, templates, and AGENTS startup guidance were updated to match, and `20 Library/Research Reports/_index.md` was created as the new default home.
# 2026-04-19

- After RI-039 passed, Jaret approved a tiny cleanup pass instead of reopening RI. The two low-risk fixes were applied: the generated run summary path was corrected from `./Process Log.md` to `../Process Log.md`, and `00 Inbox/Research Process Retest Assertions - 2026-04-18.md` was updated so the expected process-log location matches the canonical top-level `Process Log.md` pattern.
- Jaret then clarified that deep research should outperform a plain one-shot ChatGPT research pass by using self-improving search-pattern refinement as the default Mode 3 behavior, not only as a gap-filling exception. The shared research method was updated accordingly: Mode 3 now defaults to iterative refinement, the recursion note explicitly endorses taxonomy-split refinement like the red-light-helmet example, and the default bounded cap is up to 5 total cycles when another pass is still clearly earning its keep.
- Jaret then asked for a consolidation pass instead of more note sprawl. The shared research package was tightened by folding the old readiness-gates and starter-kit docs into `Research-Enabled Project Contract.md`, folding the review-workflow doc into `Research Run Review Template.md`, updating startup surfaces to load the smaller set, and adding ongoing-research continuity fields to the surviving brief, run-summary, and dossier templates.
# 2026-04-19

- After RI-039 passed, Jaret approved a tiny cleanup pass instead of reopening RI. The two low-risk fixes were applied: the generated run summary path was corrected from `./Process Log.md` to `../Process Log.md`, and `00 Inbox/Research Process Retest Assertions - 2026-04-18.md` was updated so the expected process-log location matches the canonical top-level `Process Log.md` pattern.
- Jaret then clarified that deep research should outperform a plain one-shot ChatGPT research pass by using self-improving search-pattern refinement as the default Mode 3 behavior, not only as a gap-filling exception. The shared research method was updated accordingly: Mode 3 now defaults to iterative refinement, the recursion note explicitly endorses taxonomy-split refinement like the red-light-helmet example, and the default bounded cap is up to 5 total cycles when another pass is still clearly earning its keep.
- Jaret then asked for a consolidation pass instead of more note sprawl. The shared research package was tightened by folding the old readiness-gates and starter-kit docs into `Research-Enabled Project Contract.md`, folding the review-workflow doc into `Research Run Review Template.md`, updating startup surfaces to load the smaller set, and adding ongoing-research continuity fields to the surviving brief, run-summary, and dossier templates.
- The Mode 3 robot-mower research project in `00 Inbox/Research Intake/2026-04-19 - robot mower lidar 2026/` was advanced through run-003. The key correction was that **Roborock RockMow Z120 LiDAR is a real model/SKU branch** and maps to the consumer-facing **Z1 LiDAR** family, with **X1 LiDAR** treated as the later US-facing continuation rather than a separately scored mower.
- Current corrected ranking for Jaret’s Duvall-property rubric is: **1) Mammotion LUBA 3 AWD (85.3), 2) Roborock RockMow Z120/Z1 LiDAR (84.4), 3) Dreame A3 AWD Pro 3500 (83.5), 4) Segway Navimow i2 LiDAR Pro (80.5), 5) ECOVACS GOAT A3000 LiDAR PRO (77.3), 6) MOVA LiDAX Ultra 3000 AWD (77.2)**. Bottom-line recommendation remains LUBA 3 for the exact yard, with Roborock now the main corrected #2 contender.
# 2026-04-19

- After RI-039 passed, Jaret approved a tiny cleanup pass instead of reopening RI. The two low-risk fixes were applied: the generated run summary path was corrected from `./Process Log.md` to `../Process Log.md`, and `00 Inbox/Research Process Retest Assertions - 2026-04-18.md` was updated so the expected process-log location matches the canonical top-level `Process Log.md` pattern.
- Jaret then clarified that deep research should outperform a plain one-shot ChatGPT research pass by using self-improving search-pattern refinement as the default Mode 3 behavior, not only as a gap-filling exception. The shared research method was updated accordingly: Mode 3 now defaults to iterative refinement, the recursion note explicitly endorses taxonomy-split refinement like the red-light-helmet example, and the default bounded cap is up to 5 total cycles when another pass is still clearly earning its keep.
- Jaret then asked for a consolidation pass instead of more note sprawl. The shared research package was tightened by folding the old readiness-gates and starter-kit docs into `Research-Enabled Project Contract.md`, folding the review-workflow doc into `Research Run Review Template.md`, updating startup surfaces to load the smaller set, and adding ongoing-research continuity fields to the surviving brief, run-summary, and dossier templates.
# 2026-04-19

- After RI-039 passed, Jaret approved a tiny cleanup pass instead of reopening RI. The two low-risk fixes were applied: the generated run summary path was corrected from `./Process Log.md` to `../Process Log.md`, and `00 Inbox/Research Process Retest Assertions - 2026-04-18.md` was updated so the expected process-log location matches the canonical top-level `Process Log.md` pattern.
- Jaret then clarified that deep research should outperform a plain one-shot ChatGPT research pass by using self-improving search-pattern refinement as the default Mode 3 behavior, not only as a gap-filling exception. The shared research method was updated accordingly: Mode 3 now defaults to iterative refinement, the recursion note explicitly endorses taxonomy-split refinement like the red-light-helmet example, and the default bounded cap is up to 5 total cycles when another pass is still clearly earning its keep.
- Jaret then asked for a consolidation pass instead of more note sprawl. The shared research package was tightened by folding the old readiness-gates and starter-kit docs into `Research-Enabled Project Contract.md`, folding the review-workflow doc into `Research Run Review Template.md`, updating startup surfaces to load the smaller set, and adding ongoing-research continuity fields to the surviving brief, run-summary, and dossier templates.
- The Mode 3 robot-mower research project in `00 Inbox/Research Intake/2026-04-19 - robot mower lidar 2026/` was advanced through run-003. The key correction was that **Roborock RockMow Z120 LiDAR is a real model/SKU branch** and maps to the consumer-facing **Z1 LiDAR** family, with **X1 LiDAR** treated as the later US-facing continuation rather than a separately scored mower.
- Current corrected ranking for Jaret’s Duvall-property rubric is: **1) Mammotion LUBA 3 AWD (85.3), 2) Roborock RockMow Z120/Z1 LiDAR (84.4), 3) Dreame A3 AWD Pro 3500 (83.5), 4) Segway Navimow i2 LiDAR Pro (80.5), 5) ECOVACS GOAT A3000 LiDAR PRO (77.3), 6) MOVA LiDAX Ultra 3000 AWD (77.2)**. Bottom-line recommendation remains LUBA 3 for the exact yard, with Roborock now the main corrected #2 contender.
- Jaret clarified a durable workflow preference: `00 Inbox` is appropriate for temporary intake, tests, or notes that need review/decision, but real research reports should live in `Library` rather than remaining in the inbox.
- Reviewing the robot-mower process-compliance rerun after the added second pass, the research workflow itself now effectively passes: Mode 3 was explicit, preflight artifacts came before external research, local-first grounding was documented, source captures were separate, and iterative run artifacts existed. The remaining strict assertion gap was the final handoff reply, which named artifacts but did not explicitly restate the chosen mode and rationale.
- The heartbeat-reported QMD failure on semantic query `robot mower lidar AWD edge mowing` did not reproduce locally. A direct CPU-only rerun succeeded in about 4.5 seconds with roughly 571 MB RSS, so the best current read is intermittent external `SIGKILL` or runtime/supervisor resource pressure rather than a deterministic QMD query bug. Operational takeaway: prefer `qmd search` first in heartbeat-style retrieval and escalate to `qmd query` only when semantic reranking is actually needed.
- Jaret ratified a durable storage split for research outputs: real research report packages should default to `20 Library/Research Reports/<YYYY-MM-DD - topic>/`, while `00 Inbox/Research Intake/` should be reserved for temporary process tests, temporary intake material, or explicit inbox-review routing. The shared research startup rules, templates, and AGENTS startup guidance were updated to match, and `20 Library/Research Reports/_index.md` was created as the new default home.
- The robot-mower buyer brief at `00 Inbox/Robot Mower Buyer Brief - 2026 LiDAR.md` was updated with a criteria-focused comparison table for the top three contenders, adding warranty comparison and smallest-relevant-trim pricing. Durable takeaway: Mammotion LUBA 3 AWD currently has the strongest documented warranty detail (3 years across major listed components), Roborock RockMow Z120/Z1 LiDAR has 3 years on the main unit but only 2 years on battery and accessories, and Dreame A3 AWD Pro 3500 surfaced a 3-year headline warranty but with thinner recoverable component-level documentation.- Jaret manually moved the robot-mower research package into `20 Library/Research Reports/`, aligning the live artifact location with the newly ratified default home for real research report packages.
- Final OMS cleanup sweep removed some safe note-surface bloat without changing the tested startup behavior: `Projects/Obsidian Memory System/_index.md` now points fresh readers to the shared `Operating Rules and Playbooks/Memory/_index.md` surface instead of pinning the promoted rule copies as if they were still live there, `40 Agent Nexus/_index.md` now points to the OMS folder index rather than directly pinning three historical OMS planning notes, and the unreferenced completed one-off note `40 Agent Nexus/Operating Rules and Playbooks/Ratification Audit.md` was archived to `99 Archive/Retired Plans/Ratification Audit 2026-04-18.md`.
- No startup-critical memory or research rule surfaces changed during that final cleanup sweep, so the earlier fresh-session validation results still stand and did not need another rerun.
- Jaret set a new durable presentation preference: for inbox-facing notes, final reports, and other likely-to-be-read deliverables, prefer tasteful visual markdown, especially Mermaid and Obsidian-friendly callouts, whenever it materially improves scanability. This was initially codified in `USER.md`, `AGENTS.md`, a new shared playbook note `40 Agent Nexus/Operating Rules and Playbooks/Report Presentation Rule.md`, and the research/report templates most likely to drive user-facing outputs.
- Same-day cleanup reversed the bloated parts of that rollout. The durable preference now lives in a short `USER.md` note, while the added AGENTS routing, the standalone report-presentation playbook, and the extra template/index sections were removed or archived to keep startup and shared-rule surfaces lean.
# 2026-04-19

- After RI-039 passed, Jaret approved a tiny cleanup pass instead of reopening RI. The two low-risk fixes were applied: the generated run summary path was corrected from `./Process Log.md` to `../Process Log.md`, and `00 Inbox/Research Process Retest Assertions - 2026-04-18.md` was updated so the expected process-log location matches the canonical top-level `Process Log.md` pattern.
- Jaret then clarified that deep research should outperform a plain one-shot ChatGPT research pass by using self-improving search-pattern refinement as the default Mode 3 behavior, not only as a gap-filling exception. The shared research method was updated accordingly: Mode 3 now defaults to iterative refinement, the recursion note explicitly endorses taxonomy-split refinement like the red-light-helmet example, and the default bounded cap is up to 5 total cycles when another pass is still clearly earning its keep.
- Jaret then asked for a consolidation pass instead of more note sprawl. The shared research package was tightened by folding the old readiness-gates and starter-kit docs into `Research-Enabled Project Contract.md`, folding the review-workflow doc into `Research Run Review Template.md`, updating startup surfaces to load the smaller set, and adding ongoing-research continuity fields to the surviving brief, run-summary, and dossier templates.
# 2026-04-19

- After RI-039 passed, Jaret approved a tiny cleanup pass instead of reopening RI. The two low-risk fixes were applied: the generated run summary path was corrected from `./Process Log.md` to `../Process Log.md`, and `00 Inbox/Research Process Retest Assertions - 2026-04-18.md` was updated so the expected process-log location matches the canonical top-level `Process Log.md` pattern.
- Jaret then clarified that deep research should outperform a plain one-shot ChatGPT research pass by using self-improving search-pattern refinement as the default Mode 3 behavior, not only as a gap-filling exception. The shared research method was updated accordingly: Mode 3 now defaults to iterative refinement, the recursion note explicitly endorses taxonomy-split refinement like the red-light-helmet example, and the default bounded cap is up to 5 total cycles when another pass is still clearly earning its keep.
- Jaret then asked for a consolidation pass instead of more note sprawl. The shared research package was tightened by folding the old readiness-gates and starter-kit docs into `Research-Enabled Project Contract.md`, folding the review-workflow doc into `Research Run Review Template.md`, updating startup surfaces to load the smaller set, and adding ongoing-research continuity fields to the surviving brief, run-summary, and dossier templates.
- The Mode 3 robot-mower research project in `00 Inbox/Research Intake/2026-04-19 - robot mower lidar 2026/` was advanced through run-003. The key correction was that **Roborock RockMow Z120 LiDAR is a real model/SKU branch** and maps to the consumer-facing **Z1 LiDAR** family, with **X1 LiDAR** treated as the later US-facing continuation rather than a separately scored mower.
- Current corrected ranking for Jaret’s Duvall-property rubric is: **1) Mammotion LUBA 3 AWD (85.3), 2) Roborock RockMow Z120/Z1 LiDAR (84.4), 3) Dreame A3 AWD Pro 3500 (83.5), 4) Segway Navimow i2 LiDAR Pro (80.5), 5) ECOVACS GOAT A3000 LiDAR PRO (77.3), 6) MOVA LiDAX Ultra 3000 AWD (77.2)**. Bottom-line recommendation remains LUBA 3 for the exact yard, with Roborock now the main corrected #2 contender.
# 2026-04-19

- After RI-039 passed, Jaret approved a tiny cleanup pass instead of reopening RI. The two low-risk fixes were applied: the generated run summary path was corrected from `./Process Log.md` to `../Process Log.md`, and `00 Inbox/Research Process Retest Assertions - 2026-04-18.md` was updated so the expected process-log location matches the canonical top-level `Process Log.md` pattern.
- Jaret then clarified that deep research should outperform a plain one-shot ChatGPT research pass by using self-improving search-pattern refinement as the default Mode 3 behavior, not only as a gap-filling exception. The shared research method was updated accordingly: Mode 3 now defaults to iterative refinement, the recursion note explicitly endorses taxonomy-split refinement like the red-light-helmet example, and the default bounded cap is up to 5 total cycles when another pass is still clearly earning its keep.
- Jaret then asked for a consolidation pass instead of more note sprawl. The shared research package was tightened by folding the old readiness-gates and starter-kit docs into `Research-Enabled Project Contract.md`, folding the review-workflow doc into `Research Run Review Template.md`, updating startup surfaces to load the smaller set, and adding ongoing-research continuity fields to the surviving brief, run-summary, and dossier templates.
# 2026-04-19

- After RI-039 passed, Jaret approved a tiny cleanup pass instead of reopening RI. The two low-risk fixes were applied: the generated run summary path was corrected from `./Process Log.md` to `../Process Log.md`, and `00 Inbox/Research Process Retest Assertions - 2026-04-18.md` was updated so the expected process-log location matches the canonical top-level `Process Log.md` pattern.
- Jaret then clarified that deep research should outperform a plain one-shot ChatGPT research pass by using self-improving search-pattern refinement as the default Mode 3 behavior, not only as a gap-filling exception. The shared research method was updated accordingly: Mode 3 now defaults to iterative refinement, the recursion note explicitly endorses taxonomy-split refinement like the red-light-helmet example, and the default bounded cap is up to 5 total cycles when another pass is still clearly earning its keep.
- Jaret then asked for a consolidation pass instead of more note sprawl. The shared research package was tightened by folding the old readiness-gates and starter-kit docs into `Research-Enabled Project Contract.md`, folding the review-workflow doc into `Research Run Review Template.md`, updating startup surfaces to load the smaller set, and adding ongoing-research continuity fields to the surviving brief, run-summary, and dossier templates.
- The Mode 3 robot-mower research project in `00 Inbox/Research Intake/2026-04-19 - robot mower lidar 2026/` was advanced through run-003. The key correction was that **Roborock RockMow Z120 LiDAR is a real model/SKU branch** and maps to the consumer-facing **Z1 LiDAR** family, with **X1 LiDAR** treated as the later US-facing continuation rather than a separately scored mower.
- Current corrected ranking for Jaret’s Duvall-property rubric is: **1) Mammotion LUBA 3 AWD (85.3), 2) Roborock RockMow Z120/Z1 LiDAR (84.4), 3) Dreame A3 AWD Pro 3500 (83.5), 4) Segway Navimow i2 LiDAR Pro (80.5), 5) ECOVACS GOAT A3000 LiDAR PRO (77.3), 6) MOVA LiDAX Ultra 3000 AWD (77.2)**. Bottom-line recommendation remains LUBA 3 for the exact yard, with Roborock now the main corrected #2 contender.
- Jaret clarified a durable workflow preference: `00 Inbox` is appropriate for temporary intake, tests, or notes that need review/decision, but real research reports should live in `Library` rather than remaining in the inbox.
- Reviewing the robot-mower process-compliance rerun after the added second pass, the research workflow itself now effectively passes: Mode 3 was explicit, preflight artifacts came before external research, local-first grounding was documented, source captures were separate, and iterative run artifacts existed. The remaining strict assertion gap was the final handoff reply, which named artifacts but did not explicitly restate the chosen mode and rationale.
- The heartbeat-reported QMD failure on semantic query `robot mower lidar AWD edge mowing` did not reproduce locally. A direct CPU-only rerun succeeded in about 4.5 seconds with roughly 571 MB RSS, so the best current read is intermittent external `SIGKILL` or runtime/supervisor resource pressure rather than a deterministic QMD query bug. Operational takeaway: prefer `qmd search` first in heartbeat-style retrieval and escalate to `qmd query` only when semantic reranking is actually needed.
- Jaret ratified a durable storage split for research outputs: real research report packages should default to `20 Library/Research Reports/<YYYY-MM-DD - topic>/`, while `00 Inbox/Research Intake/` should be reserved for temporary process tests, temporary intake material, or explicit inbox-review routing. The shared research startup rules, templates, and AGENTS startup guidance were updated to match, and `20 Library/Research Reports/_index.md` was created as the new default home.
# 2026-04-19

- After RI-039 passed, Jaret approved a tiny cleanup pass instead of reopening RI. The two low-risk fixes were applied: the generated run summary path was corrected from `./Process Log.md` to `../Process Log.md`, and `00 Inbox/Research Process Retest Assertions - 2026-04-18.md` was updated so the expected process-log location matches the canonical top-level `Process Log.md` pattern.
- Jaret then clarified that deep research should outperform a plain one-shot ChatGPT research pass by using self-improving search-pattern refinement as the default Mode 3 behavior, not only as a gap-filling exception. The shared research method was updated accordingly: Mode 3 now defaults to iterative refinement, the recursion note explicitly endorses taxonomy-split refinement like the red-light-helmet example, and the default bounded cap is up to 5 total cycles when another pass is still clearly earning its keep.
- Jaret then asked for a consolidation pass instead of more note sprawl. The shared research package was tightened by folding the old readiness-gates and starter-kit docs into `Research-Enabled Project Contract.md`, folding the review-workflow doc into `Research Run Review Template.md`, updating startup surfaces to load the smaller set, and adding ongoing-research continuity fields to the surviving brief, run-summary, and dossier templates.
# 2026-04-19

- After RI-039 passed, Jaret approved a tiny cleanup pass instead of reopening RI. The two low-risk fixes were applied: the generated run summary path was corrected from `./Process Log.md` to `../Process Log.md`, and `00 Inbox/Research Process Retest Assertions - 2026-04-18.md` was updated so the expected process-log location matches the canonical top-level `Process Log.md` pattern.
- Jaret then clarified that deep research should outperform a plain one-shot ChatGPT research pass by using self-improving search-pattern refinement as the default Mode 3 behavior, not only as a gap-filling exception. The shared research method was updated accordingly: Mode 3 now defaults to iterative refinement, the recursion note explicitly endorses taxonomy-split refinement like the red-light-helmet example, and the default bounded cap is up to 5 total cycles when another pass is still clearly earning its keep.
- Jaret then asked for a consolidation pass instead of more note sprawl. The shared research package was tightened by folding the old readiness-gates and starter-kit docs into `Research-Enabled Project Contract.md`, folding the review-workflow doc into `Research Run Review Template.md`, updating startup surfaces to load the smaller set, and adding ongoing-research continuity fields to the surviving brief, run-summary, and dossier templates.
- The Mode 3 robot-mower research project in `00 Inbox/Research Intake/2026-04-19 - robot mower lidar 2026/` was advanced through run-003. The key correction was that **Roborock RockMow Z120 LiDAR is a real model/SKU branch** and maps to the consumer-facing **Z1 LiDAR** family, with **X1 LiDAR** treated as the later US-facing continuation rather than a separately scored mower.
- Current corrected ranking for Jaret’s Duvall-property rubric is: **1) Mammotion LUBA 3 AWD (85.3), 2) Roborock RockMow Z120/Z1 LiDAR (84.4), 3) Dreame A3 AWD Pro 3500 (83.5), 4) Segway Navimow i2 LiDAR Pro (80.5), 5) ECOVACS GOAT A3000 LiDAR PRO (77.3), 6) MOVA LiDAX Ultra 3000 AWD (77.2)**. Bottom-line recommendation remains LUBA 3 for the exact yard, with Roborock now the main corrected #2 contender.
# 2026-04-19

- After RI-039 passed, Jaret approved a tiny cleanup pass instead of reopening RI. The two low-risk fixes were applied: the generated run summary path was corrected from `./Process Log.md` to `../Process Log.md`, and `00 Inbox/Research Process Retest Assertions - 2026-04-18.md` was updated so the expected process-log location matches the canonical top-level `Process Log.md` pattern.
- Jaret then clarified that deep research should outperform a plain one-shot ChatGPT research pass by using self-improving search-pattern refinement as the default Mode 3 behavior, not only as a gap-filling exception. The shared research method was updated accordingly: Mode 3 now defaults to iterative refinement, the recursion note explicitly endorses taxonomy-split refinement like the red-light-helmet example, and the default bounded cap is up to 5 total cycles when another pass is still clearly earning its keep.
- Jaret then asked for a consolidation pass instead of more note sprawl. The shared research package was tightened by folding the old readiness-gates and starter-kit docs into `Research-Enabled Project Contract.md`, folding the review-workflow doc into `Research Run Review Template.md`, updating startup surfaces to load the smaller set, and adding ongoing-research continuity fields to the surviving brief, run-summary, and dossier templates.
- The Mode 3 robot-mower research project in `00 Inbox/Research Intake/2026-04-19 - robot mower lidar 2026/` was advanced through run-003. The key correction was that **Roborock RockMow Z120 LiDAR is a real model/SKU branch** and maps to the consumer-facing **Z1 LiDAR** family, with **X1 LiDAR** treated as the later US-facing continuation rather than a separately scored mower.
- Current corrected ranking for Jaret’s Duvall-property rubric is: **1) Mammotion LUBA 3 AWD (85.3), 2) Roborock RockMow Z120/Z1 LiDAR (84.4), 3) Dreame A3 AWD Pro 3500 (83.5), 4) Segway Navimow i2 LiDAR Pro (80.5), 5) ECOVACS GOAT A3000 LiDAR PRO (77.3), 6) MOVA LiDAX Ultra 3000 AWD (77.2)**. Bottom-line recommendation remains LUBA 3 for the exact yard, with Roborock now the main corrected #2 contender.
- Jaret clarified a durable workflow preference: `00 Inbox` is appropriate for temporary intake, tests, or notes that need review/decision, but real research reports should live in `Library` rather than remaining in the inbox.
- Reviewing the robot-mower process-compliance rerun after the added second pass, the research workflow itself now effectively passes: Mode 3 was explicit, preflight artifacts came before external research, local-first grounding was documented, source captures were separate, and iterative run artifacts existed. The remaining strict assertion gap was the final handoff reply, which named artifacts but did not explicitly restate the chosen mode and rationale.
- The heartbeat-reported QMD failure on semantic query `robot mower lidar AWD edge mowing` did not reproduce locally. A direct CPU-only rerun succeeded in about 4.5 seconds with roughly 571 MB RSS, so the best current read is intermittent external `SIGKILL` or runtime/supervisor resource pressure rather than a deterministic QMD query bug. Operational takeaway: prefer `qmd search` first in heartbeat-style retrieval and escalate to `qmd query` only when semantic reranking is actually needed.
- Jaret ratified a durable storage split for research outputs: real research report packages should default to `20 Library/Research Reports/<YYYY-MM-DD - topic>/`, while `00 Inbox/Research Intake/` should be reserved for temporary process tests, temporary intake material, or explicit inbox-review routing. The shared research startup rules, templates, and AGENTS startup guidance were updated to match, and `20 Library/Research Reports/_index.md` was created as the new default home.
- The robot-mower buyer brief at `00 Inbox/Robot Mower Buyer Brief - 2026 LiDAR.md` was updated with a criteria-focused comparison table for the top three contenders, adding warranty comparison and smallest-relevant-trim pricing. Durable takeaway: Mammotion LUBA 3 AWD currently has the strongest documented warranty detail (3 years across major listed components), Roborock RockMow Z120/Z1 LiDAR has 3 years on the main unit but only 2 years on battery and accessories, and Dreame A3 AWD Pro 3500 surfaced a 3-year headline warranty but with thinner recoverable component-level documentation.- Jaret manually moved the robot-mower research package into `20 Library/Research Reports/`, aligning the live artifact location with the newly ratified default home for real research report packages.
- Final OMS cleanup sweep removed some safe note-surface bloat without changing the tested startup behavior: `Projects/Obsidian Memory System/_index.md` now points fresh readers to the shared `Operating Rules and Playbooks/Memory/_index.md` surface instead of pinning the promoted rule copies as if they were still live there, `40 Agent Nexus/_index.md` now points to the OMS folder index rather than directly pinning three historical OMS planning notes, and the unreferenced completed one-off note `40 Agent Nexus/Operating Rules and Playbooks/Ratification Audit.md` was archived to `99 Archive/Retired Plans/Ratification Audit 2026-04-18.md`.
- No startup-critical memory or research rule surfaces changed during that final cleanup sweep, so the earlier fresh-session validation results still stand and did not need another rerun.
- Jaret set a new durable presentation preference: for inbox-facing notes, final reports, and other likely-to-be-read deliverables, prefer tasteful visual markdown, especially Mermaid and Obsidian-friendly callouts, whenever it materially improves scanability. This was initially codified in `USER.md`, `AGENTS.md`, a new shared playbook note `40 Agent Nexus/Operating Rules and Playbooks/Report Presentation Rule.md`, and the research/report templates most likely to drive user-facing outputs.
- Same-day cleanup reversed the bloated parts of that rollout. The durable preference now lives in a short `USER.md` note, while the added AGENTS routing, the standalone report-presentation playbook, and the extra template/index sections were removed or archived to keep startup and shared-rule surfaces lean.
- Idea Factory was fully reframed into a presentation-first daily research loop: the project now maintains `Opportunity Table.md` plus `Opportunity State Ledger.md`, writes a clipped inbox ranking brief and a separate featured-opportunity memo each day, stores full featured memos under `20 Library/Research Reports/2026-04-19 - Idea Factory Market Research/Featured Opportunities/`, and uses the inbox only as the skim layer with a pointer.
- The live native cron job `[CRON-IDEA-FACTORY-DAILY]` was upgraded so daily runs explicitly read the state ledger and featured-report template, select a featured row by movement/freshness/decision value rather than static rank alone, update featured-state metadata, write the full memo into the research track, and keep Discord quiet on normal completion.
- Idea Factory now has durable control notes for the presentation stack and scope boundaries: `Featured Opportunity Presentation North Star.md`, `Featured Opportunity Report Template.md`, `Featured Opportunity Rendering Path.md`, `Delivery Surface Roadmap.md`, and `Research vs Launch Automation Boundary.md`. The current recommendation is to keep `00 Inbox/` as the primary delivery surface for now, treat Gmail only as a later summary-only push layer, and treat a lightweight read-only web surface as the more promising next major derived presentation layer.
- As of the end of this heartbeat run chain, the Idea Factory local board is effectively clear. Only `IF-017` remains in backlog, explicitly gated until 7 completed daily presentation-first runs exist. Operationally, the project’s `every-heartbeat plus daily native cron` registry cadence likely needs to be downgraded once the control plane is updated, because the daily cron now carries the real ongoing work and heartbeat dispatch has no ungated local card left.
