# 2026-04-17

- Jaret clarified that Idea Factory should not begin external market research until the proper research infrastructure is in place, including tool selection and the useful borrowed pieces of GBrain and auto-research.
- Split the work into a new Obsidian project, `Research Infrastructure`, under `40 Agent Nexus/Projects/Research Infrastructure/`, with a project brief, stack plan, local backlog, and local Kanban.
- Added concrete Research Infrastructure notes for retrieval routing, a compiled-truth-plus-timeline-style research dossier template, a source-capture template, and a minimal auto-research loop.
- Ran a local dry-run validation using the existing GBrain study note to confirm the new source-capture to dossier flow works cleanly without beginning Idea Factory market research.
- Reframed `Idea Factory` as infrastructure-gated: the market scan card is now blocked on the Research Infrastructure dependency, and the global Kanban now tracks the infrastructure prerequisite work as the active P1 item.
# 2026-04-17

- Jaret clarified that Idea Factory should not begin external market research until the proper research infrastructure is in place, including tool selection and the useful borrowed pieces of GBrain and auto-research.
- Split the work into a new Obsidian project, `Research Infrastructure`, under `40 Agent Nexus/Projects/Research Infrastructure/`, with a project brief, stack plan, local backlog, and local Kanban.
- Added concrete Research Infrastructure notes for retrieval routing, a compiled-truth-plus-timeline-style research dossier template, a source-capture template, and a minimal auto-research loop.
- Ran a local dry-run validation using the existing GBrain study note to confirm the new source-capture to dossier flow works cleanly without beginning Idea Factory market research.
- Reframed `Idea Factory` as infrastructure-gated: the market scan card is now blocked on the Research Infrastructure dependency, and the global Kanban now tracks the infrastructure prerequisite work as the active P1 item.
- Jaret asked for a serious study of `garrytan/gbrain` focused on what to borrow for the existing memory system rather than adopting it blindly.
- Wrote the review memo to `/mnt/c/Users/Jaret/Obsidian/The Nexus/00 Inbox/GBrain study report 2026-04-16.md` after reading the repo docs and core implementation files.
- Main conclusion from the GBrain review: do not replace the current Markdown-first memory system wholesale; borrow selected ideas instead, especially explicit `brain vs memory vs session` separation, `compiled truth + timeline` note structure, stronger retrieval-routing discipline, and possibly a later database-backed retrieval index.
- Key architectural takeaway preserved for future work: in GBrain, Postgres + pgvector are used as part of a broader hybrid retrieval stack, not as the sole source of truth, and Obsidian can remain the canonical human-facing markdown layer while a Postgres/PGLite + pgvector layer acts as a secondary machine-facing index.
- After Jaret reviewed the GBrain memo, the recommendation was refined toward a research-first but still lightweight design: keep markdown and Obsidian canonical, keep QMD as the first conceptual recall layer, move the ingest -> enrich -> search -> update -> sync loop into Phase 1, and treat skill-like markdown playbooks plus bounded maintenance as justified now while deferring any Postgres/PGLite + pgvector index until scale proves QMD insufficient.
- Associated the `Research Infrastructure` project with Discord channel `#research-infrastructure` (`agent:main:discord:channel:1494843033194336406`) and updated the Project Registry routing entry accordingly.
- Clarified the project boundary between `Obsidian Memory System` and `Research Infrastructure`: shared memory-platform architecture now explicitly belongs to Obsidian Memory System, while Research Infrastructure is the downstream research-specific consumer.
- Moved the misfiled inbox notes `GBrain study report 2026-04-16.md` and `Revised GBrain adoption proposal 2026-04-17.md` into `40 Agent Nexus/Projects/Obsidian Memory System/`, moved the remaining inbox project notes into `Idea Factory/`, added a new `Project Brief.md` for Obsidian Memory System, and added `Memory Platform Dependency and Handoff.md` to Research Infrastructure so the research thread can inherit the right architectural context without owning the whole platform.
# 2026-04-17

- Jaret clarified that Idea Factory should not begin external market research until the proper research infrastructure is in place, including tool selection and the useful borrowed pieces of GBrain and auto-research.
- Split the work into a new Obsidian project, `Research Infrastructure`, under `40 Agent Nexus/Projects/Research Infrastructure/`, with a project brief, stack plan, local backlog, and local Kanban.
- Added concrete Research Infrastructure notes for retrieval routing, a compiled-truth-plus-timeline-style research dossier template, a source-capture template, and a minimal auto-research loop.
- Ran a local dry-run validation using the existing GBrain study note to confirm the new source-capture to dossier flow works cleanly without beginning Idea Factory market research.
- Reframed `Idea Factory` as infrastructure-gated: the market scan card is now blocked on the Research Infrastructure dependency, and the global Kanban now tracks the infrastructure prerequisite work as the active P1 item.
# 2026-04-17

- Jaret clarified that Idea Factory should not begin external market research until the proper research infrastructure is in place, including tool selection and the useful borrowed pieces of GBrain and auto-research.
- Split the work into a new Obsidian project, `Research Infrastructure`, under `40 Agent Nexus/Projects/Research Infrastructure/`, with a project brief, stack plan, local backlog, and local Kanban.
- Added concrete Research Infrastructure notes for retrieval routing, a compiled-truth-plus-timeline-style research dossier template, a source-capture template, and a minimal auto-research loop.
- Ran a local dry-run validation using the existing GBrain study note to confirm the new source-capture to dossier flow works cleanly without beginning Idea Factory market research.
- Reframed `Idea Factory` as infrastructure-gated: the market scan card is now blocked on the Research Infrastructure dependency, and the global Kanban now tracks the infrastructure prerequisite work as the active P1 item.
- Jaret asked for a serious study of `garrytan/gbrain` focused on what to borrow for the existing memory system rather than adopting it blindly.
- Wrote the review memo to `/mnt/c/Users/Jaret/Obsidian/The Nexus/00 Inbox/GBrain study report 2026-04-16.md` after reading the repo docs and core implementation files.
- Main conclusion from the GBrain review: do not replace the current Markdown-first memory system wholesale; borrow selected ideas instead, especially explicit `brain vs memory vs session` separation, `compiled truth + timeline` note structure, stronger retrieval-routing discipline, and possibly a later database-backed retrieval index.
- Key architectural takeaway preserved for future work: in GBrain, Postgres + pgvector are used as part of a broader hybrid retrieval stack, not as the sole source of truth, and Obsidian can remain the canonical human-facing markdown layer while a Postgres/PGLite + pgvector layer acts as a secondary machine-facing index.
- After Jaret reviewed the GBrain memo, the recommendation was refined toward a research-first but still lightweight design: keep markdown and Obsidian canonical, keep QMD as the first conceptual recall layer, move the ingest -> enrich -> search -> update -> sync loop into Phase 1, and treat skill-like markdown playbooks plus bounded maintenance as justified now while deferring any Postgres/PGLite + pgvector index until scale proves QMD insufficient.
- Associated the `Research Infrastructure` project with Discord channel `#research-infrastructure` (`agent:main:discord:channel:1494843033194336406`) and updated the Project Registry routing entry accordingly.
- Clarified the project boundary between `Obsidian Memory System` and `Research Infrastructure`: shared memory-platform architecture now explicitly belongs to Obsidian Memory System, while Research Infrastructure is the downstream research-specific consumer.
- Moved the misfiled inbox notes `GBrain study report 2026-04-16.md` and `Revised GBrain adoption proposal 2026-04-17.md` into `40 Agent Nexus/Projects/Obsidian Memory System/`, moved the remaining inbox project notes into `Idea Factory/`, added a new `Project Brief.md` for Obsidian Memory System, and added `Memory Platform Dependency and Handoff.md` to Research Infrastructure so the research thread can inherit the right architectural context without owning the whole platform.
- Correction for the end-of-day cleanup pass: the durable work actually completed in this turn was creating `/mnt/c/Users/Jaret/Obsidian/The Nexus/40 Agent Nexus/Projects/Obsidian Memory System/Project Brief.md` and `/mnt/c/Users/Jaret/Obsidian/The Nexus/40 Agent Nexus/Projects/Research Infrastructure/Memory Platform Dependency and Handoff.md` to make the project boundary explicit.
- The inbox-note moves and any Project Registry updates mentioned in chat were not actually executed yet at flush time and should be treated as still pending verification/completion in the next session.
- Cleaned the active stale vault-path references by updating `20 Library/OpenClaw Reference/My OpenClaw Setup Guide.md` to use the live canonical vault root `C:\Users\Jaret\Obsidian\The Nexus` / `/mnt/c/Users/Jaret/Obsidian/The Nexus` and to explicitly reject the retired `/mnt/g/...` Google Drive path for live use; remaining `/mnt/g` references are now either archival or explicit warnings not to use that path.
- Turned the revised GBrain-based Phase 1 idea into an executable note at `40 Agent Nexus/Projects/Obsidian Memory System/Phase 1 Action Plan.md`, splitting shared memory-platform ownership from research-specific execution.
- Added the missing Research Infrastructure execution notes: `Ingestion Playbook.md`, `Enrichment Playbook.md`, `Reporting Playbook.md`, and `Maintenance Playbook.md`, and generalized `Research Dossier Template.md` so it no longer hardcodes `Idea Factory` as the project name.
- Updated the Research Infrastructure project surfaces and registry to reflect the new state: the hybrid-index pilot is now deferred by default until real retrieval pain appears, and the remaining blocker before the first live external run is just the first downstream-project input package (approved source list, approved research themes/questions, and rubric binding when ranking is required).
- Read through the updated Research Infrastructure project docs, then decomposed the remaining GBrain-inspired implementation work into concrete Research Infrastructure cards: tool-stack and run-input contract, bounded run procedure, recurring-subject dossier rule, downstream run-readiness gates, and a more explicitly deferred hybrid-index re-evaluation trigger.
- Refined the Research Infrastructure plan so GBrain is now explicitly treated as the first implementation slice, not the whole project: after the GBrain-first baseline, the backlog now stages a broader agentic-research data-collection phase, an inbox timing proposal for Jaret, and a later comprehensive stack design phase covering workflows, tools, repos, skills, and recursive research methods.
- Expanded that discovery scope beyond my first pass by adding a dedicated `Agentic Research Discovery Scope.md` note and broader backlog coverage for source-surface access, X and YouTube transcript paths, OpenClaw-native capability gaps, workflow families, evaluation criteria, operational constraints, and pre-design sequencing before the comprehensive research-stack plan is drafted.
- Tightened the Research Infrastructure GBrain slice into a real contract by rewriting `GBrain-First Baseline.md` with explicit tool roles, workflow contract, required output artifacts, completion criteria, and non-goals, then reconciled the project surfaces so RI-010 stays done and RI-014 is the active source-surface audit card.
- Jaret clarified a standing safety rule for research work: treat all external data as untrusted read-only input and stay wary of prompt-injection attempts. For delegation, Max should only own local-only/internal analysis tasks, not external research tasks. Reflected this in the Research Infrastructure backlog and baseline notes by assigning RI-015 to Max and keeping external-source research with Johnny.
- Promoted that rule from project-local guidance into workspace-wide policy by updating `AGENTS.md` so external data is globally treated as untrusted read-only evidence and prompt-injection resistance is an explicit standing rule, not just a Research Infrastructure convention.
- Jaret corrected the sequencing: the Revised GBrain adoption proposal should be implemented first in the `Obsidian Memory System` project as shared memory-platform work, Research Infrastructure should define the research-specific tools and algorithms on top of that package, and only after both upstream projects are complete should Idea Factory move into run planning.
- Corrected the project surfaces and boards accordingly: Obsidian Memory System now explicitly owns the shared Phase 1 package, Research Infrastructure is framed as the downstream tool/procedure/algorithm layer, and Idea Factory is blocked on completion of both upstream projects rather than on any immediate run-input package.
- Implemented the core shared Obsidian Memory System Phase 1 notes in the live vault: `Brain vs Memory vs Session Rule.md`, `Shared Retrieval Routing Rule.md`, `Provenance Convention.md`, and `Compiled Truth + Timeline Note Pattern.md`, then updated the Obsidian Memory System `Phase 1 Action Plan.md`, `Project Brief.md`, `_index.md`, and `Current State.md` so those notes are now the canonical shared package.
- Fixed the live QMD Obsidian collection paths after the vault move. `agent-nexus` and `daily-logs` now point at `/mnt/c/Users/Jaret/Obsidian/The Nexus/...`, `qmd` is confirmed installed globally, and plain exact searches like `qmd search "project timeline"` now work against the live vault-backed collections again. The unused `openclaw-space` collection was then removed from QMD after confirming its target folder does not exist in the current vault.
- Expanded the QMD vault corpus to cover the remaining included numbered folders: `00 Inbox`, `10 Spaces`, `20 Library`, and full `70 History`, while continuing to exclude `01 System`, `.trash`, and `99 Archive`. Replaced the narrower `daily-logs` collection with `history` to avoid duplicate results, verified `qmd search` now hits the expanded vault corpus, and started a fresh embedding pass so semantic search can use the new folders too.
- Added a native daily 5:30 AM America/Los_Angeles QMD refresh cron in the main session. It runs `qmd update` and then `QMD_LLAMA_GPU=off qmd embed`, stays silent on success, and only surfaces a blocker if the maintenance fails.
- Correction: Jaret's follow-up note was that the gateway had been reset/restarted, not a new reminder about daemonization. The systemd-managed gateway status remains separately verified from `openclaw gateway status` / `openclaw status`, but the chat correction itself was about the reset.
