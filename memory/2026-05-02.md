# 2026-05-02

- Idea Factory automation/coverage improvement: after Jaret asked how daily research lenses can be automated without losing judgment, created a lens coverage system. After mobile/Obsidian link resolution made a separate `Lens Coverage Matrix.md` unreliable, removed the required separate-note link and folded the canonical matrix inline into `Daily Smart Research Loop.md`. The active lens taxonomy is 7 core lenses: Explore, Drill down, Validate demand, Validate monetization, Check freshness, Challenge, and Feature / conviction pass. Source surfaces stay in `Source Rotation Registry.md`. The superseded separate matrix note was archived under `Research Prep/Lens Coverage Matrix - superseded.md`; the accidental same-named root-level duplicate was backed up outside the vault and removed.
- Idea Factory lead capture from Jaret's X post: fetched `https://x.com/i/status/2049141839710867710` via fxtwitter because direct X fetch failed, captured it as `Sources/2026-05-02 - Josh Peleg Solo Founder App Categories X Post.md`, and added incubator rows for niche professional AI utility wrappers, creator/faceless-content workflow microtools, habit/accountability niche app, and vertical niche fitness companion. The utility-wrapper row is explicitly framed as a drill-down path for the broader micro-SaaS / workflow-kit cluster. Therapy-adjacent was treated as risky due to privacy/safety/claims burden.
- Discord speak/voice command investigation: current config has `channels.discord.commands.native=false`, `channels.discord.commands.nativeSkills=false`, and `channels.discord.voice.enabled=false`; docs say Discord `/vc` requires both native commands and `channels.discord.voice`. Logs show these were intentionally applied during the 2026-05-01 cleanup/rollback work and the gateway then cleared native commands (`commands.native=false`). Systemd also has `OPENCLAW_DISCORD_SKIP_NATIVE_DEPLOY=1`, so even if config is changed, native slash command deployment may be skipped until that env override is removed/restarted.
- Discord outage follow-up: after Jaret restarted gateway at 09:46, current `~/.openclaw/openclaw.json` still has `channels.discord.commands.native=false`, `nativeSkills=false`, and `voice.enabled=false`; the attempted restore created `backups/discord-voice-restore-20260502-093955` and removed `OPENCLAW_DISCORD_SKIP_NATIVE_DEPLOY=1` from `node22.conf`, but did not change `openclaw.json`. The running process still has `OPENCLAW_DISCORD_SKIP_NATIVE_DEPLOY=1` in `/proc/<pid>/environ` even though the current unit files no longer do, so one more restart after a real config fix should drop it. Logs around restart show Discord failing to reach READY due Discord gateway fetch/DNS failures (`EAI_AGAIN gateway.discord.gg`, `fetch failed`, `failed to clear native commands`), then auto-restarts; GitHub has active related issues #73978 (plugin slash commands 3s ack/Unknown interaction), #39825/#65039 (`/vc` join aborts), and #75888 (slashCommandDeploy mode), but local config/runtime state is the immediate blocker.
- Discord repair outcome: after router restart, network probes to Discord/OpenAI/OpenRouter recovered. Final working OpenClaw Discord config: `channels.discord.commands.native=true`, `channels.discord.commands.nativeSkills=false`, `channels.discord.voice.enabled=true`; `OPENCLAW_DISCORD_SKIP_NATIVE_DEPLOY` is absent from the running gateway env. `openclaw channels status --probe` reported Discord default enabled/configured/running/connected/works, and Jaret confirmed normal Discord replies plus slash commands returned. Important lesson: do not enable `nativeSkills=true` as a broad restore; it registered/deployed many skill commands and increased startup/deploy risk. Prefer minimal native commands first, then opt into skill commands only deliberately.
