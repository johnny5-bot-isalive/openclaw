# 2026-05-01

- Idea Factory package delivery issue: Jaret reported the daily idea did not arrive in `#idea-factory` / inbox. The package stage had started but had not completed the canonical inbox/daily-ranking/featured-report artifacts. Repaired by landing `00 Inbox/Idea Factory 2026-05-01.md`, daily ranking, featured report, run summary, and table/ledger/tracker/source-rotation updates.
- 2026-05-01 featured Idea Factory opportunity: **AI feature intelligence product for micro-SaaS**. Promoted score 71→74, rank 13→7 based on Seeto solo-founder SaaS competitive-intelligence pricing, RivalSense weekly competitor-signal proof, and directional Upwork snippets. Keep wedge narrow: micro-SaaS feature-adoption intelligence, not generic competitor monitoring.
- Idea Factory failure investigation: 2026-04-30 failed because the old one-piece `[CRON-IDEA-FACTORY-DAILY]` timed out after partially writing table/source artifacts but before the inbox package. 2026-05-01 failed differently: the new package writer emitted progress text plus tool calls (`I have everything needed...`) and the cron marked that early text as a delivered successful summary after only early control-file writes. Hardened `[CRON-IDEA-FACTORY-PACKAGE]` to run isolated on `chatgpt-5.5`, forbid progress text before tool completion, require verification before final reply, and added `[CRON-IDEA-FACTORY-PACKAGE-VALIDATE]` at 7:05 AM PT to alert if the inbox/ranking/featured/run-summary package is missing or inconsistent.
- Jaret corrected the OpenClaw update-review format: release notes in `00 Inbox` should be recommendation briefs, not note dumps. They must explain how the release affects this setup, call out opt-in config decisions with recommendations, identify workflow/memory/config implications, and include post-update smoke tests. Updated `MEMORY.md`, the daily update-check cron prompt, and `00 Inbox/OpenClaw Update Review.md` accordingly.
- With Jaret's approval, installed OpenClaw `2026.4.29` via an external `systemd-run --user` invocation because `openclaw update` refuses to run directly from inside the gateway service process. Verified afterward with `openclaw update status` and `openclaw --version`: pnpm is up to date at `2026.4.29` (`a448042`). Gateway restart is still pending for Jaret to perform.
- Vault-access troubleshooting after heartbeat concern: direct reads of `/mnt/c/Users/Jaret/Obsidian/The Nexus/40 Agent Nexus/Project Registry.md` and `Kanban.md` work from both `read` and shell; all 5 registry project folder/backlog/kanban targets resolve and are readable. The only live retrieval fault found was QMD: `better-sqlite3` had been compiled for Node ABI 141 while runtime Node 22 needs ABI 127. Rebuilt QMD's `better-sqlite3`, ran `qmd update`, ran CPU-only `qmd embed`, and verified `qmd search "Global Kanban" -c agent-nexus` returns `qmd://agent-nexus/kanban.md`. Native `memory_search` remains broken separately with `ansiStyles.color.ansi is not a function`.
- OpenClaw incident after 2026.4.29 update: Discord latency is caused by gateway event-loop starvation/high CPU, not vault file permissions. Evidence: gateway process ~60% CPU/900MB RSS, `openclaw gateway probe` connects but read probe times out, logs show stream prep 62-68s (`core-plugin-tools` ~26-32s), session locks >400s, Discord heartbeat ACK timeouts, and memory sync failing with `ansiStyles.color.ansi is not a function`. Vault files and QMD exact search are readable from WSL.
- OpenClaw cleanup pass: applied only the safe `messages.groupChat.visibleReplies = "automatic"` config change after backing up `/home/jaret/.openclaw/openclaw.json` to `/home/jaret/.openclaw/openclaw.json.bak.20260501-151612-pre-cleanup`; config validated and gateway did not restart (PID stayed `601137`). Prepared but did not apply the heavier Phase 1 cleanup because it requires/risks gateway restart: disable Discord native command/native skill sync and Discord voice, and set `plugins.allow` to `acpx`, `browser`, `device-pair`, `diffs`, `discord`, `document-extract`, `file-transfer`, `google`, `memory-core`, `minimax`, `openai`, `tavily`, `web-readability`, `xai`, reducing enabled plugins from 72 to 14 in a temp-config dry run.
- Fixed native `memory_search`: root cause was OpenClaw runtime deps resolving `slice-ansi@7/8` used by `node-llama-cpp` against incompatible `ansi-styles@4.3.0`; installed `ansi-styles@6.2.3` under the affected `slice-ansi`/`stdout-update` dependency subtrees, verified `node-llama-cpp` import, `openclaw memory status --agent main --deep --index --json`, and live `memory_search` returning results. Gateway process was already restarted afterward (PID start 15:25 PDT), so the tool picked up the fixed deps.
- Jaret rolled OpenClaw back to `2026.4.27` because `2026.4.29` matched active GitHub regressions: Discord heartbeat ACK drops (#75769), 30–100s pre-response latency (#75752), gateway CPU/event-loop saturation (#75707/#75759), bundled channel plugin dependency failures (#75818), and Active Memory main-lane wedging (#75843). Updated the daily update-check cron job `a75171dd-8b55-468a-9ef7-d8a8127b0d19` to require a canonical `openclaw/openclaw` GitHub regression search before approval prompts, and rewrote `00 Inbox/OpenClaw Update Review.md` to recommend deferring `2026.4.29` until a safer release exists.
