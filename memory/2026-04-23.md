# 2026-04-23

- Mission Control `MC-012` is complete. The React shell in `/home/jaret/repos/mission-control/frontend/src/App.tsx` and `frontend/src/App.css` is now split into real `Overview`, `Projects`, and `Actions` surfaces instead of one mixed launchpad, while existing action flows and API contracts stayed intact.
- `MC-012` was validated with `npm --prefix frontend run typecheck` and `npm --prefix frontend run build`.
- After `MC-012` landed, the Mission Control local board was reconciled: `MC-012` moved to `Done`, and newly unblocked cards `MC-014`, `MC-016`, and `MC-017` were pulled from backlog into `Ready` alongside `MC-013` and `MC-018`.
- Mission Control local board state after reconciliation: `Ready` = `MC-013`, `MC-014`, `MC-016`, `MC-017`, `MC-018`; backlog now only retains gated cards `MC-015` and `MC-020`.
# 2026-04-23

- Mission Control `MC-012` is complete. The React shell in `/home/jaret/repos/mission-control/frontend/src/App.tsx` and `frontend/src/App.css` is now split into real `Overview`, `Projects`, and `Actions` surfaces instead of one mixed launchpad, while existing action flows and API contracts stayed intact.
- `MC-012` was validated with `npm --prefix frontend run typecheck` and `npm --prefix frontend run build`.
- After `MC-012` landed, the Mission Control local board was reconciled: `MC-012` moved to `Done`, and newly unblocked cards `MC-014`, `MC-016`, and `MC-017` were pulled from backlog into `Ready` alongside `MC-013` and `MC-018`.
- Mission Control local board state after reconciliation: `Ready` = `MC-013`, `MC-014`, `MC-016`, `MC-017`, `MC-018`; backlog now only retains gated cards `MC-015` and `MC-020`.
- Later on 2026-04-23, Mission Control completed the redesign tranche through `MC-019`: `MC-013` delivered the collapsible icon rail, `MC-014` delivered the centered 3-card Projects carousel, `MC-015` folded focused-project details into the selected carousel card, `MC-016` simplified the Actions tab into a compact button-first selector with lean preview/run panels, `MC-017` turned Overview into a calmer operational summary, `MC-018` replaced the old blue-heavy styling with a neutral dark palette, and `MC-019` hardened frontend API URL composition plus live `Create Project` verification against a disposable vault.
- Current Mission Control state at flush time: only `MC-020` remains active on the local board, with no blocker. The ongoing polish pass has already improved contextual tab copy, carousel cues, nav icon clarity and focus feedback, project context in Actions, no-project warning flows, direct links from Actions into markdown project notes, correct optional-focus behavior for non-project actions like `Create Project`, and neutral styling across the remaining action/form/result surfaces.
- `MC-013` through `MC-020` work continued to validate cleanly with repeated `npm --prefix frontend run typecheck` and `npm --prefix frontend run build` runs after each slice.
- Later on 2026-04-23, Mission Control added a new archive/state-management slice under `MC-021`. `/home/jaret/repos/mission-control/backend/Controllers/ActionsController.cs` now exposes `POST /api/actions/set-project-active-state`, `POST /api/actions/archive-project/preview`, and `POST /api/actions/archive-project`; `/home/jaret/repos/mission-control/backend/Controllers/ProjectsController.cs` now respects `MISSION_CONTROL_VAULT_ROOT` and keeps inactive projects visible while excluding archived ones; and `/home/jaret/repos/mission-control/frontend/src/App.tsx` plus `App.css` now show a small active/inactive toggle directly on project cards plus a new `Archive Project` action with preview and an explicit browser `Are you sure?` confirmation. Disposable-vault validation on a temporary backend confirmed active -> inactive state change, cadence drop to `manual`, archive preview, registry row removal, registry note cleanup, folder move into `99 Archive/Project Archive/`, and frontmatter status rewrite to `archived`. The backend build plus frontend typecheck/build all passed, with the latest frontend build emitting `dist/assets/index-Cxt3Vm_u.css` and `dist/assets/index-DTtChklA.js`.
- Later still on 2026-04-23, Mission Control landed a broader backend verification slice under `MC-022`. I added a disposable-vault integration harness in `/home/jaret/repos/mission-control/backend.tests/` and expanded coverage across status, project discovery, create-project preview/execute, activate-sprint preview/execute, active-state toggling, archive preview/execute, run-project-checks, helper-dispatch preview, and helper-dispatch execution through a deterministic fake `codex` shim placed on `PATH` during the test. The new Activate Sprint tests exposed a real parser bug in `ActionsController.cs`: the backlog trailing-footer regex was broad enough to swallow later `###` card headings as footer content. Tightening that regex fixed the bug, and the final validation pass succeeded with 13 passing tests in `/home/jaret/.dotnet/dotnet test backend.tests/backend.tests.csproj`, plus `/home/jaret/.dotnet/dotnet build backend/backend.csproj`, `npm --prefix frontend run typecheck`, and `npm --prefix frontend run build`.
# 2026-04-23

- Mission Control `MC-012` is complete. The React shell in `/home/jaret/repos/mission-control/frontend/src/App.tsx` and `frontend/src/App.css` is now split into real `Overview`, `Projects`, and `Actions` surfaces instead of one mixed launchpad, while existing action flows and API contracts stayed intact.
- `MC-012` was validated with `npm --prefix frontend run typecheck` and `npm --prefix frontend run build`.
- After `MC-012` landed, the Mission Control local board was reconciled: `MC-012` moved to `Done`, and newly unblocked cards `MC-014`, `MC-016`, and `MC-017` were pulled from backlog into `Ready` alongside `MC-013` and `MC-018`.
- Mission Control local board state after reconciliation: `Ready` = `MC-013`, `MC-014`, `MC-016`, `MC-017`, `MC-018`; backlog now only retains gated cards `MC-015` and `MC-020`.
# 2026-04-23

- Mission Control `MC-012` is complete. The React shell in `/home/jaret/repos/mission-control/frontend/src/App.tsx` and `frontend/src/App.css` is now split into real `Overview`, `Projects`, and `Actions` surfaces instead of one mixed launchpad, while existing action flows and API contracts stayed intact.
- `MC-012` was validated with `npm --prefix frontend run typecheck` and `npm --prefix frontend run build`.
- After `MC-012` landed, the Mission Control local board was reconciled: `MC-012` moved to `Done`, and newly unblocked cards `MC-014`, `MC-016`, and `MC-017` were pulled from backlog into `Ready` alongside `MC-013` and `MC-018`.
- Mission Control local board state after reconciliation: `Ready` = `MC-013`, `MC-014`, `MC-016`, `MC-017`, `MC-018`; backlog now only retains gated cards `MC-015` and `MC-020`.
- Later on 2026-04-23, Mission Control completed the redesign tranche through `MC-019`: `MC-013` delivered the collapsible icon rail, `MC-014` delivered the centered 3-card Projects carousel, `MC-015` folded focused-project details into the selected carousel card, `MC-016` simplified the Actions tab into a compact button-first selector with lean preview/run panels, `MC-017` turned Overview into a calmer operational summary, `MC-018` replaced the old blue-heavy styling with a neutral dark palette, and `MC-019` hardened frontend API URL composition plus live `Create Project` verification against a disposable vault.
- Current Mission Control state at flush time: only `MC-020` remains active on the local board, with no blocker. The ongoing polish pass has already improved contextual tab copy, carousel cues, nav icon clarity and focus feedback, project context in Actions, no-project warning flows, direct links from Actions into markdown project notes, correct optional-focus behavior for non-project actions like `Create Project`, and neutral styling across the remaining action/form/result surfaces.
- `MC-013` through `MC-020` work continued to validate cleanly with repeated `npm --prefix frontend run typecheck` and `npm --prefix frontend run build` runs after each slice.
- Later on 2026-04-23, Mission Control added a new archive/state-management slice under `MC-021`. `/home/jaret/repos/mission-control/backend/Controllers/ActionsController.cs` now exposes `POST /api/actions/set-project-active-state`, `POST /api/actions/archive-project/preview`, and `POST /api/actions/archive-project`; `/home/jaret/repos/mission-control/backend/Controllers/ProjectsController.cs` now respects `MISSION_CONTROL_VAULT_ROOT` and keeps inactive projects visible while excluding archived ones; and `/home/jaret/repos/mission-control/frontend/src/App.tsx` plus `App.css` now show a small active/inactive toggle directly on project cards plus a new `Archive Project` action with preview and an explicit browser `Are you sure?` confirmation. Disposable-vault validation on a temporary backend confirmed active -> inactive state change, cadence drop to `manual`, archive preview, registry row removal, registry note cleanup, folder move into `99 Archive/Project Archive/`, and frontmatter status rewrite to `archived`. The backend build plus frontend typecheck/build all passed, with the latest frontend build emitting `dist/assets/index-Cxt3Vm_u.css` and `dist/assets/index-DTtChklA.js`.
- Later still on 2026-04-23, Mission Control landed a broader backend verification slice under `MC-022`. I added a disposable-vault integration harness in `/home/jaret/repos/mission-control/backend.tests/` and expanded coverage across status, project discovery, create-project preview/execute, activate-sprint preview/execute, active-state toggling, archive preview/execute, run-project-checks, helper-dispatch preview, and helper-dispatch execution through a deterministic fake `codex` shim placed on `PATH` during the test. The new Activate Sprint tests exposed a real parser bug in `ActionsController.cs`: the backlog trailing-footer regex was broad enough to swallow later `###` card headings as footer content. Tightening that regex fixed the bug, and the final validation pass succeeded with 13 passing tests in `/home/jaret/.dotnet/dotnet test backend.tests/backend.tests.csproj`, plus `/home/jaret/.dotnet/dotnet build backend/backend.csproj`, `npm --prefix frontend run typecheck`, and `npm --prefix frontend run build`.
- Later on 2026-04-23, `MC-020` kept moving after the archive/state slice. The simplified Projects-first frontend got a control-feel cleanup focused on the new project-state and archive affordances: the active/inactive toggle on cards was made smaller and calmer, gained a subtle state dot for faster scanning, and the archive workspace got clearer destructive-path treatment with stronger compact warning copy plus a dedicated danger-styled execute button. The frontend still passed `npm --prefix frontend run typecheck` and `npm --prefix frontend run build`, with the latest build emitting `dist/assets/index-B8eHOdWd.css` and `dist/assets/index-BoXklHuI.js`.
