# 2026-04-18

- Jaret clarified the correct operational interpretation for the overnight project-dispatch discrepancy:
  - Research Infrastructure (RI): execution mostly works, but reply delivery and session-history visibility are flaky. Treat RI timeouts as a transport/reporting problem first, not immediate evidence that the project session failed to do work. Trust RI file and board changes over timeout behavior, and describe it as "project work succeeded, reply path flaky" when needed.
  - Obsidian Memory System (OMS): dispatch has not been the main source of progress. OMS changes mostly came from main or Max directly, while the OMS project session mostly just confirmed that the local board was clear. Do not over-credit OMS dispatch as productive execution; describe OMS as "local board remained clear; actual OMS completions were handled outside the dispatched project session" when needed.
- OpenClaw routing config was updated to try to unblock sibling project dispatch: `tools.agentToAgent.enabled=true` and `tools.sessions.visibility="agent"`. Even after restart, heartbeat still showed project-dispatch timeout symptoms, so treat the remaining problem as timeout/acknowledgment behavior rather than assuming pure reachability failure.
- Important heartbeat diagnosis from transcript review: `sessions_send` timeouts can coexist with real downstream project progress. In particular, RI often received the wake and completed work after the control-plane send hit its 45s timeout. Future diagnosis should distinguish "delivery failed" from "reply wait expired too soon" before reporting routing as broken.
- Follow-up fix: updated `HEARTBEAT.md` and `AGENTS.md` so project-session dispatches now default to at least a 120-second wait, and stale handoffs must re-read local board files plus check session history before being called routing failures.
- `G-028` was not actually removed at the time it was said to be removed; it remained on the global Kanban in `Done` as a historical global wrapper for RI work. Avoid claiming a card is gone until the live board is re-read and confirmed.
- Jaret explicitly wants anything sent to `00 Inbox` to be a tight, clear executive brief. Keep inbox notes short by default, and link to deeper project files when more detail is needed.
# 2026-04-18

- Jaret clarified the correct operational interpretation for the overnight project-dispatch discrepancy:
  - Research Infrastructure (RI): execution mostly works, but reply delivery and session-history visibility are flaky. Treat RI timeouts as a transport/reporting problem first, not immediate evidence that the project session failed to do work. Trust RI file and board changes over timeout behavior, and describe it as "project work succeeded, reply path flaky" when needed.
  - Obsidian Memory System (OMS): dispatch has not been the main source of progress. OMS changes mostly came from main or Max directly, while the OMS project session mostly just confirmed that the local board was clear. Do not over-credit OMS dispatch as productive execution; describe OMS as "local board remained clear; actual OMS completions were handled outside the dispatched project session" when needed.
- OpenClaw routing config was updated to try to unblock sibling project dispatch: `tools.agentToAgent.enabled=true` and `tools.sessions.visibility="agent"`. Even after restart, heartbeat still showed project-dispatch timeout symptoms, so treat the remaining problem as timeout/acknowledgment behavior rather than assuming pure reachability failure.
- Important heartbeat diagnosis from transcript review: `sessions_send` timeouts can coexist with real downstream project progress. In particular, RI often received the wake and completed work after the control-plane send hit its 45s timeout. Future diagnosis should distinguish "delivery failed" from "reply wait expired too soon" before reporting routing as broken.
- Follow-up fix: updated `HEARTBEAT.md` and `AGENTS.md` so project-session dispatches now default to at least a 120-second wait, and stale handoffs must re-read local board files plus check session history before being called routing failures.
- `G-028` was not actually removed at the time it was said to be removed; it remained on the global Kanban in `Done` as a historical global wrapper for RI work. Avoid claiming a card is gone until the live board is re-read and confirmed.
- Jaret explicitly wants anything sent to `00 Inbox` to be a tight, clear executive brief. Keep inbox notes short by default, and link to deeper project files when more detail is needed.
- OMS local follow-through is effectively complete for now: repeated heartbeat checks confirmed the OMS local Kanban and backlog are clear after the post-Phase-1 cleanup, so future OMS work should only be reopened when real retrieval, provenance, or standing-note maintenance pain appears.
- The first bounded Idea Factory market-scan brief is now prepared, but the remaining blocker before the first external read-only market scan is explicit Jaret approval. Research Infrastructure cadence was reduced away from `every-heartbeat` once only the deferred RI-005 trigger remained.
- Jaret wants quota exhaustion or similar core-service failures surfaced explicitly instead of being quietly worked around. If memory-search quota is exhausted, inform Jaret promptly so he can decide whether to top up, switch providers, or approve a local fallback.
- The local fallback change applied today affects OpenClaw `memory_search` embeddings only. It does not by itself change `web_search`, `web_fetch`, or Gemini-powered generation/tooling outside memory retrieval.
- Jaret's preferred search split is now explicit: keep `web_search` on Tavily as the normal provider, allow Gemini web search to remain available only as fallback, and keep local memory retrieval on the free local embeddings path if quality remains good enough.
# 2026-04-18

- Jaret clarified the correct operational interpretation for the overnight project-dispatch discrepancy:
  - Research Infrastructure (RI): execution mostly works, but reply delivery and session-history visibility are flaky. Treat RI timeouts as a transport/reporting problem first, not immediate evidence that the project session failed to do work. Trust RI file and board changes over timeout behavior, and describe it as "project work succeeded, reply path flaky" when needed.
  - Obsidian Memory System (OMS): dispatch has not been the main source of progress. OMS changes mostly came from main or Max directly, while the OMS project session mostly just confirmed that the local board was clear. Do not over-credit OMS dispatch as productive execution; describe OMS as "local board remained clear; actual OMS completions were handled outside the dispatched project session" when needed.
- OpenClaw routing config was updated to try to unblock sibling project dispatch: `tools.agentToAgent.enabled=true` and `tools.sessions.visibility="agent"`. Even after restart, heartbeat still showed project-dispatch timeout symptoms, so treat the remaining problem as timeout/acknowledgment behavior rather than assuming pure reachability failure.
- Important heartbeat diagnosis from transcript review: `sessions_send` timeouts can coexist with real downstream project progress. In particular, RI often received the wake and completed work after the control-plane send hit its 45s timeout. Future diagnosis should distinguish "delivery failed" from "reply wait expired too soon" before reporting routing as broken.
- Follow-up fix: updated `HEARTBEAT.md` and `AGENTS.md` so project-session dispatches now default to at least a 120-second wait, and stale handoffs must re-read local board files plus check session history before being called routing failures.
- `G-028` was not actually removed at the time it was said to be removed; it remained on the global Kanban in `Done` as a historical global wrapper for RI work. Avoid claiming a card is gone until the live board is re-read and confirmed.
- Jaret explicitly wants anything sent to `00 Inbox` to be a tight, clear executive brief. Keep inbox notes short by default, and link to deeper project files when more detail is needed.
# 2026-04-18

- Jaret clarified the correct operational interpretation for the overnight project-dispatch discrepancy:
  - Research Infrastructure (RI): execution mostly works, but reply delivery and session-history visibility are flaky. Treat RI timeouts as a transport/reporting problem first, not immediate evidence that the project session failed to do work. Trust RI file and board changes over timeout behavior, and describe it as "project work succeeded, reply path flaky" when needed.
  - Obsidian Memory System (OMS): dispatch has not been the main source of progress. OMS changes mostly came from main or Max directly, while the OMS project session mostly just confirmed that the local board was clear. Do not over-credit OMS dispatch as productive execution; describe OMS as "local board remained clear; actual OMS completions were handled outside the dispatched project session" when needed.
- OpenClaw routing config was updated to try to unblock sibling project dispatch: `tools.agentToAgent.enabled=true` and `tools.sessions.visibility="agent"`. Even after restart, heartbeat still showed project-dispatch timeout symptoms, so treat the remaining problem as timeout/acknowledgment behavior rather than assuming pure reachability failure.
- Important heartbeat diagnosis from transcript review: `sessions_send` timeouts can coexist with real downstream project progress. In particular, RI often received the wake and completed work after the control-plane send hit its 45s timeout. Future diagnosis should distinguish "delivery failed" from "reply wait expired too soon" before reporting routing as broken.
- Follow-up fix: updated `HEARTBEAT.md` and `AGENTS.md` so project-session dispatches now default to at least a 120-second wait, and stale handoffs must re-read local board files plus check session history before being called routing failures.
- `G-028` was not actually removed at the time it was said to be removed; it remained on the global Kanban in `Done` as a historical global wrapper for RI work. Avoid claiming a card is gone until the live board is re-read and confirmed.
- Jaret explicitly wants anything sent to `00 Inbox` to be a tight, clear executive brief. Keep inbox notes short by default, and link to deeper project files when more detail is needed.
- OMS local follow-through is effectively complete for now: repeated heartbeat checks confirmed the OMS local Kanban and backlog are clear after the post-Phase-1 cleanup, so future OMS work should only be reopened when real retrieval, provenance, or standing-note maintenance pain appears.
- The first bounded Idea Factory market-scan brief is now prepared, but the remaining blocker before the first external read-only market scan is explicit Jaret approval. Research Infrastructure cadence was reduced away from `every-heartbeat` once only the deferred RI-005 trigger remained.
- Jaret wants quota exhaustion or similar core-service failures surfaced explicitly instead of being quietly worked around. If memory-search quota is exhausted, inform Jaret promptly so he can decide whether to top up, switch providers, or approve a local fallback.
- The local fallback change applied today affects OpenClaw `memory_search` embeddings only. It does not by itself change `web_search`, `web_fetch`, or Gemini-powered generation/tooling outside memory retrieval.
- Jaret's preferred search split is now explicit: keep `web_search` on Tavily as the normal provider, allow Gemini web search to remain available only as fallback, and keep local memory retrieval on the free local embeddings path if quality remains good enough.
- Jaret explicitly wants bounded recursive refinement treated as part of the research method: start broad when needed, then allow capped gap-closing follow-up passes that refine questions and source choices as understanding improves. Defer only unbounded self-improving loops, not bounded recursion itself.
- Cross-project durable rules and procedures should not remain trapped inside finite project folders. The long-term pattern should be: projects incubate rules, a shared evergreen capability layer in The Nexus becomes the durable home, and `AGENTS.md` should point fresh sessions to that shared home.
- Added a global backlog item, `G-033`, to define and implement the shared evergreen home for durable operating rules and capability playbooks that outlive individual projects.
- Jaret wants backlog items written for fresh-agent handoff: plain, unambiguous titles, a concrete next action, and explicit references to the project notes or rule files needed to start cleanly without guesswork.
# 2026-04-18

- Final OMS closeout comparison against the live files found no remaining implementation gap, only documentation drift. The durable state to remember is: the four shared OMS Phase 1 rules now truly live canonically in `40 Agent Nexus/Operating Rules and Playbooks/`, the OMS project-folder copies are historical-reference pointers, and OMS currently has no open local execution card.
- The OMS project docs were reconciled to match reality: `Project Brief.md`, `Current State.md`, and `Phase 1 Action Plan.md` now reflect that Phase 1 is complete, the evergreen operating-rules folder is the shared canonical home, and future OMS work should reopen only when real shared-memory pain or a new promotion-worthy cross-project rule appears.
- RI state correction from Jaret after a slightly stale heartbeat summary: `RI-028`, `RI-029`, and `RI-030` are done; `Research Brief Template.md` and `Research Run Summary Template.md` exist; `RI-031` is the current `Doing` card; and the RI `_index.md` has been updated to link the new templates. Treat this as the current no-blocker RI baseline.
- RI baseline correction from Jaret one step later: `RI-031` is now also done, `Research Run Review Template.md` exists, `RI-032` is the current `Doing` card, the RI `_index.md` is updated, and there is still no RI blocker.
- RI baseline correction from Jaret one more step later: `RI-032` is now also done, `Downstream Research Project Starter Kit.md` exists, `RI-033` is the current `Doing` card, the RI `_index.md` is updated, and there is still no RI blocker.
- RI correction from Jaret after another stale heartbeat summary: `RI-033` is still the current `Doing` card, and the promoted evergreen tranche now also explicitly includes `Downstream Handoff and Run-Readiness Gates.md`, `Recurring Subject Dossier Rule.md`, `Research Run Review Workflow and Scorecard.md`, and `Unsupported and Blocked Source Exception Handling.md`; the shared `Operating Rules and Playbooks/_index.md` has been updated again, the RI-local source copies now carry canonical-home pointers, and there is still no RI blocker.
- RI correction from Jaret one step later: `RI-033` and `RI-034` are both done, `RI-035` is now the current `Doing` card, and the RI local brief, `_index.md`, and `Implementation Stage Plan.md` now point cleanly at `Operating Rules and Playbooks` as the canonical shared home; there is still no RI blocker.

- Research Infrastructure reached an implementation-ready milestone: `RI-035` was completed via `Validation/Generic Downstream-Ready Validation 2026-04-18.md`, and the generic walkthrough found no blocking structural gap in the shared evergreen RI package.
- Current RI local state after validation: no `Doing`, no `Ready`, and only gated backlog item `RI-005` remains, so RI should now be treated as effectively clear unless new research-method work is explicitly added or real downstream retrieval pain appears.
- The shared evergreen research capability layer under `40 Agent Nexus/Operating Rules and Playbooks/` is now validated as the canonical downstream-ready entry point for research briefs, bounded runs, run reviews, starter-kit setup, and related method rules.
- Jaret ran a fresh-session inbox-style research test and found a real enforcement failure: the agent could partially understand the research method yet still drift into a normal helpful answer instead of producing the required compliant research package. Treat this as a workflow-enforcement gap, not just a documentation gap.
- In response, the research startup hooks were tightened in `AGENTS.md` and the shared playbooks: the brief is now explicitly the mandatory preflight artifact, a default one-off landing zone under `00 Inbox/Research Intake/<YYYY-MM-DD - topic>/` was defined, a shared `Source Capture Template.md` was added, the rules now explicitly forbid the first real external search before artifacts and gates exist, and a closeout compliance check is now required before presenting findings.
- RI is no longer "clear". A new card, `RI-037, Re-test fresh-session research compliance against an inbox-style run`, is now the next ready step. `RI-036, Add hard execution scaffolding for fresh-session research compliance`, is complete.
- Jaret’s delegation preference was codified more explicitly in `AGENTS.md`: default repetitive low-risk grunt work to Max when practical, while keeping external-source research and high-judgment synthesis with Johnny unless there is a clear reason to delegate otherwise.
- Jaret clarified an important research-output preference: when a topic is large enough, a single report dropped in the inbox is not enough. He wants a collection of sources plus iterative research reports in a topic-dedicated research folder. In response, the shared method now includes `Research Task Sizing and Escalation Rule.md`, which distinguishes quick answers from bounded runs and dedicated topic research tracks.
- Jaret also proposed a simple phrase hook: when he explicitly asks for a "research project", default that request to the dedicated-topic Mode 3 path unless he clearly narrows it back down. This is now codified in `AGENTS.md` and the shared sizing rule.
- Jaret raised a good cold-start concern: telling agents to "check Operating Rules and Playbooks" is too ambiguous once that folder grows large. In response, a narrow research-specific startup surface now exists at `40 Agent Nexus/Operating Rules and Playbooks/Research/_index.md`, and `AGENTS.md` now points there instead of at the whole mixed folder.
- Jaret then said he would prefer the research-specific shared files to live physically inside that `Research/` subfolder for sanity. That move has now been done: the shared research rules and templates were relocated into `40 Agent Nexus/Operating Rules and Playbooks/Research/`, indexes were cleaned up, and stale old-path references were updated.
- RI-037 was executed as a true fresh-session retest and produced a useful but failed result. The fresh session did create a brief, folder, source note, and run summary, but it still chose Mode 2 instead of Mode 3 after the explicit phrase "research project", skipped or weakly documented local-first grounding, batched many external sources into one capture note, used a weak process log, and landed the topic folder in the workspace `00 Inbox` instead of the Nexus inbox. The failure is now documented in `Projects/Research Infrastructure/Validation/Fresh Session Compliance Retest 2026-04-18.md`, and RI-038 is the new active card to fix those exposed gaps.
- RI-038 is now complete. The shared research package was hardened again by adding `Research Process Log Template.md`, wiring process-log requirements into `AGENTS.md`, the research startup index, the brief, the run procedure, the starter kit, and local RI traceability copies, strengthening the Mode 3 phrase hook, requiring one source-capture note per meaningful external source, making local-first grounding and closeout compliance checks more explicit, and pinning the one-off research intake landing zone to the Nexus inbox path. The next RI card is `RI-039`, a rerun of the fresh-session compliance retest.
- RI-039 then reran the fresh-session inbox-style compliance test and passed on the targeted fixes. The fresh session now chose Mode 3 correctly from the phrase hook, created artifacts in the Nexus inbox landing zone, documented local-first grounding, produced a real process log, and left multiple source-capture notes instead of one blended memo. Minor cleanup notes remain around exact source-count consistency and a relative process-log path in the run summary, but the major fresh-session enforcement gap is now considered closed and RI is effectively clear again except for gated `RI-005`.
