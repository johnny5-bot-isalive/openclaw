# MEMORY.md

Long-term curated memory for this workspace.

- OpenClaw is running as a systemd-managed user service in this environment.
- Native OpenClaw cron is configured for a daily 5:30 AM America/Los_Angeles QMD refresh, a daily 9:00 AM update approval prompt, a 12:10 AM daily retrospective, a 3:00 AM Dreaming sweep, a 5:00 AM workspace sync, and a weekly Sunday 9:30 AM `MEMORY.md` hygiene review.
- The daily update check now runs as an isolated cron job that checks `openclaw update status` and announces in `#mission-control` only when an update is available, so Jaret gets an explicit approval prompt before any manual update happens.
- The workspace sync is an overnight docs-focused commit-and-push job for `/home/jaret/repos/openclaw-workspace`; it prioritizes pushing the current workspace state to `origin` and stays silent unless there is a blocker.
- Obsidian is implemented as a durable second-memory and collaboration layer, with plain Markdown as the system of record and plugins treated as optional accelerators.
- Jaret prefers workflow and memory to remain markdown-first. When building companion web tools, prefer thin action/launcher surfaces that read from or trigger against the markdown-based system rather than creating a second authoritative workflow store that needs constant sync.
- The active Obsidian vault root for the memory project is `C:\Users\Jaret\Obsidian\The Nexus` on Windows, which maps to `/mnt/c/Users/Jaret/Obsidian/The Nexus` in WSL/Linux. This Windows-backed vault path should be treated as durable setup memory and reused instead of re-asking. Do not rely on the retired Google Drive mount path or the abandoned WSL-local shadow copy.
- MiniMax M2.7 is configured as the default subagent model for lower-cost grunt work, while the main agent default remains `openai-codex/gpt-5.4`.
- Codex is the default ACP coding harness in this environment (`acp.defaultAgent: "codex"`), and Jaret wants Codex treated as the default coding-agent path unless there is a clear reason to use something else.
- For external coding tasks via Codex CLI or similar harnesses, use a blocking event-driven execution pattern: plan once with a complete prompt, dispatch one blocking tool call, do not poll or emit intermediate check-ins while it runs, and only resume reasoning after the tool returns. If more work is needed, start a new one-shot cycle from the returned result.
- Execution-lane rule: use Max for mostly mechanical non-coding grunt work, use a one-shot blocking Codex call for a single bounded coding step, and use a Codex-backed ACP session or thread for larger multi-step coding work that benefits from isolated ongoing context without bloating the main chat.
- Max is the named MiniMax grunt-work subagent and should be the default choice for repetitive setup, cleanup, indexing, evaluation passes, and other low-risk execution-heavy tasks unless there is a clear reason to keep the work with Johnny directly.
- QMD is now adopted as a CLI-first local retrieval layer for workspace and Obsidian notes on this machine. Default to CPU-only usage (`QMD_LLAMA_GPU=off`). Use QMD for fuzzy semantic cross-note search, `memory_search` for native runtime recall, and ripgrep for exact lookup. QMD is not a source of truth, and MCP exposure is deferred for now.
- For X.com research/search tasks, use the dedicated `grok` subagent and have that subagent use the `x_search` tool.
- Jaret approved the future workflow direction of keeping one control-plane heartbeat while moving active projects toward project-scoped sessions and local boards. The durable plan lives in Obsidian under `40 Agent Nexus/Projects/Project-Scoped Workflow/`, with OpenCode Setup designated as the first pilot once the registry/template layer is built.
- After every approved OpenClaw update, Johnny should study the official release notes, look for setup-relevant capabilities or behavior changes worth using, persist the useful takeaways, and include a brief release-notes summary in the post-update confirmation.
- OpenClaw 2026.4.24 is installed. Relevant takeaways for this setup: Google Meet is now a bundled participant plugin with personal Google auth and artifact/attendance export support; realtime voice loops can hand off Talk, Voice Call, and Meet interactions to the full agent for deeper tool-backed answers; browser automation got steadier tab reuse plus coordinate clicks and longer action budgets; the 2026.4.24 release specifically fixes heartbeat scheduler overflow loops and improves crashy gateway restart continuation delivery; packaged/plugin runtime dependency handling was hardened again; and MCP one-shot/session runtimes now clean themselves up more reliably.
- .NET SDK 8.0.420 is installed locally for Jaret under `~/.dotnet` in this WSL environment, with `DOTNET_ROOT` and `PATH` wiring added to `~/.profile` and `~/.bashrc` so new shells can use `dotnet` without a system-wide apt install.
- The current full-stack starter repo is `/home/jaret/repos/aspnet-react-starter`, validated as an ASP.NET Core 8 + React/Vite baseline with a working `/api/status` handshake, local `make setup` / `make ci` / `make dev` flows, and GitHub Actions CI.

## Promoted From Short-Term Memory (2026-04-26)

<!-- openclaw-memory-promotion:memory:memory/2026-04-17.md:10:16 -->
- - Jaret clarified that Idea Factory should not begin external market research until the proper research infrastructure is in place, including tool selection and the useful borrowed pieces of GBrain and auto-research. - Split the work into a new Obsidian project, `Research Infrastructure`, under `40 Agent Nexus/Projects/Research Infrastructure/`, with a project brief, stack plan, local backlog, and local Kanban. - Added concrete Research Infrastructure notes for retrieval routing, a compiled-truth-plus-timeline-style research dossier template, a source-capture template, and a minimal auto-research loop. - Ran a local dry-run validation using the existing GBrain study note to confirm the new source-capture to dossier flow works cleanly without beginning Idea Factory market research. - Reframed `Idea Factory` as infrastructure-gated: the market scan card is now blocked on the Research Infrastructure dependency, and the global Kanban now tracks the infrastructure prerequisite work as the active P1 item. - Jaret asked for a serious study of `garrytan/gbrain` focused on what to borrow for the existing memory system rather than adopting it blindly. - Wrote the review memo to `/mnt/c/Users/Jaret/Obsidian/The Nexus/00 Inbox/GBrain study report 2026-04-16.md` after reading the repo docs and core implementation files. [score=0.846 recalls=14 avg=0.493 source=memory/2026-04-17.md:10-16]
<!-- openclaw-memory-promotion:memory:memory/2026-04-17.md:1:11 -->
- # 2026-04-17 - Jaret clarified that Idea Factory should not begin external market research until the proper research infrastructure is in place, including tool selection and the useful borrowed pieces of GBrain and auto-research. - Split the work into a new Obsidian project, `Research Infrastructure`, under `40 Agent Nexus/Projects/Research Infrastructure/`, with a project brief, stack plan, local backlog, and local Kanban. - Added concrete Research Infrastructure notes for retrieval routing, a compiled-truth-plus-timeline-style research dossier template, a source-capture template, and a minimal auto-research loop. - Ran a local dry-run validation using the existing GBrain study note to confirm the new source-capture to dossier flow works cleanly without beginning Idea Factory market research. - Reframed `Idea Factory` as infrastructure-gated: the market scan card is now blocked on the Research Infrastructure dependency, and the global Kanban now tracks the infrastructure prerequisite work as the active P1 item. # 2026-04-17 - Jaret clarified that Idea Factory should not begin external market research until the proper research infrastructure is in place, including tool selection and the useful borrowed pieces of GBrain and auto-research. - Split the work into a new Obsidian project, `Research Infrastructure`, under `40 Agent Nexus/Projects/Research Infrastructure/`, with a project brief, stack plan, local backlog, and local Kanban. [score=0.846 recalls=22 avg=0.491 source=memory/2026-04-17.md:1-11]
<!-- openclaw-memory-promotion:memory:memory/2026-04-17.md:26:34 -->
- - Split the work into a new Obsidian project, `Research Infrastructure`, under `40 Agent Nexus/Projects/Research Infrastructure/`, with a project brief, stack plan, local backlog, and local Kanban. - Added concrete Research Infrastructure notes for retrieval routing, a compiled-truth-plus-timeline-style research dossier template, a source-capture template, and a minimal auto-research loop. - Ran a local dry-run validation using the existing GBrain study note to confirm the new source-capture to dossier flow works cleanly without beginning Idea Factory market research. - Reframed `Idea Factory` as infrastructure-gated: the market scan card is now blocked on the Research Infrastructure dependency, and the global Kanban now tracks the infrastructure prerequisite work as the active P1 item. # 2026-04-17 - Jaret clarified that Idea Factory should not begin external market research until the proper research infrastructure is in place, including tool selection and the useful borrowed pieces of GBrain and auto-research. - Split the work into a new Obsidian project, `Research Infrastructure`, under `40 Agent Nexus/Projects/Research Infrastructure/`, with a project brief, stack plan, local backlog, and local Kanban. - Added concrete Research Infrastructure notes for retrieval routing, a compiled-truth-plus-timeline-style research dossier template, a source-capture template, and a minimal auto-research loop. [score=0.845 recalls=26 avg=0.490 source=memory/2026-04-17.md:26-34]
<!-- openclaw-memory-promotion:memory:memory/2026-04-11.md:13:16 -->
- - Heartbeat behavior was completed and normalized: `HEARTBEAT.md` now directs heartbeat to work active Johnny-owned Kanban items first and, if none exist, pull the next eligible backlog item into the Kanban and start it immediately. - After reviewing the local QMD planning notes, the global backlog was expanded with the remaining QMD implementation items needed for full functionality: corpus definition, install/init, collection/context setup, indexing/embedding, retrieval evaluation, operational decision, and downstream workflow integration. - Backlog ownership was then refined so Max is the default owner for QMD grunt-work tasks like install/init, collection setup, indexing/embedding, and retrieval evaluation, while Johnny retains architectural and policy decisions. [score=0.844 recalls=20 avg=0.496 source=memory/2026-04-11.md:13-16]
<!-- openclaw-memory-promotion:memory:memory/2026-04-17.md:20:27 -->
- - Associated the `Research Infrastructure` project with Discord channel `#research-infrastructure` (`agent:main:discord:channel:1494843033194336406`) and updated the Project Registry routing entry accordingly. - Clarified the project boundary between `Obsidian Memory System` and `Research Infrastructure`: shared memory-platform architecture now explicitly belongs to Obsidian Memory System, while Research Infrastructure is the downstream research-specific consumer. - Moved the misfiled inbox notes `GBrain study report 2026-04-16.md` and `Revised GBrain adoption proposal 2026-04-17.md` into `40 Agent Nexus/Projects/Obsidian Memory System/`, moved the remaining inbox project notes into `Idea Factory/`, added a new `Project Brief.md` for Obsidian Memory System, and added `Memory Platform Dependency and Handoff.md` to Research Infrastructure so the research thread can inherit the right architectural context without owning the whole platform. # 2026-04-17 - Jaret clarified that Idea Factory should not begin external market research until the proper research infrastructure is in place, including tool selection and the useful borrowed pieces of GBrain and auto-research. - Split the work into a new Obsidian project, `Research Infrastructure`, under `40 Agent Nexus/Projects/Research Infrastructure/`, with a project brief, stack plan, local backlog, and local Kanban. - Added concrete Research Infrastructure notes for retrieval routing, a compiled-truth-plus-timeline-style research dossier template, a source-capture template, and a minimal auto-research loop. [score=0.843 recalls=10 avg=0.493 source=memory/2026-04-17.md:20-27]
<!-- openclaw-memory-promotion:memory:memory/2026-04-11.md:5:10 -->
- - A named MiniMax cleanup worker, Max, was used successfully for repeated Obsidian implementation passes; effective orchestration requires giving subagents explicit policy context, portability rules, current project state, and the expectation that Max should absorb grunt work by default when suitable. - Graph strategy for the vault was refined after a failed first attempt: regular notes should prefer portable Markdown links, `_index.md` files are hidden structural folder contracts rather than graph hubs, and only a small number of explicitly named MOCs should act as visible graph hubs when justified. - The assistant identified a memory-system weakness: durable context existed across workspace files and Obsidian, but execution commitments like QMD research and pending updates were not enforced strongly enough; follow-up work should add a small active-obligations layer and stronger heartbeat/task enforcement after the updater and QMD commitments. - QMD research remains an explicit next commitment, targeting https://github.com/tobi/qmd for implementation planning. - Memory-system hardening should avoid over-customization and bloat: prefer native Active Memory for retrieval, keep local workspace memory lightweight and execution-focused, use Obsidian for durable collaborative/project memory, and avoid duplication across both layers unless the benefit is clear. - Obsidian vault path must be remembered exactly: Windows `G:\My Drive\Obsidian Vaults\Second Brain`, WSL `/mnt/g/My Drive/Obsidian Vaults/Second Brain`. [score=0.843 recalls=21 avg=0.491 source=memory/2026-04-11.md:5-10]
<!-- openclaw-memory-promotion:memory:memory/2026-04-10.md:1:11 -->
- # 2026-04-10 ## Obsidian Memory System - Continued implementing the Obsidian vault as a portable Markdown-first second-memory system. - Established project-management notes, shared Nexus notes, Johnny-specific working/handoff notes, and key `_index.md` files. - Confirmed the operating direction: Obsidian complements native OpenClaw memory rather than replacing it. - MiniMax M2.7 is now configured as the default subagent model, while the main agent default was restored to `openai-codex/gpt-5.4`. - Began using a named MiniMax cleanup worker, Max, for Obsidian grunt work passes. - Current standing priorities beyond Obsidian: fix the pending OpenClaw update/install path and research QMD with an implementation plan. - QMD research target repo: https://github.com/tobi/qmd [score=0.842 recalls=24 avg=0.490 source=memory/2026-04-10.md:1-11]
<!-- openclaw-memory-promotion:memory:memory/2026-04-14.md:6:12 -->
- - Heartbeat board access to the Obsidian Second Brain vault recovered after earlier ENODEV mount failures; Kanban and Backlog were readable again later in the day. - OpenCode Setup work entered the global board on 2026-04-14. G-028 was completed: OpenCode ACP is reachable from the WSL-hosted OpenClaw gateway with no immediate auth or adapter blocker. - Max pulled G-029 into Doing: verify GitHub tooling and repo-home readiness for OpenCode work. Current blocker is that `gh auth` was not logged in. Next step is `gh auth login`, then verify `gh auth status`, confirm `/home/jaret/repos/` readiness, and prepare a clean local repo path. - For the current GitHub setup flow, the recommended path is HTTPS rather than SSH, and when `gh auth login` asks "Authenticate Git with your GitHub credentials?" the recommended answer is yes. - After G-029, the next backlog items are G-030 (repo baseline for OpenCode), G-031 (enable Diffs plugin for review-oriented editing), and G-032 (later evaluate optional OpenCode hardening after at least one successful repo workflow). - Later on 2026-04-14, G-029 was completed and the earlier `gh auth` blocker was cleared. Durable state: `gh auth` is active on github.com as `johnny5-bot-isalive` with scopes `gist`, `read:org`, `repo`, and `workflow`. - The dedicated local repo home for OpenCode-managed work is now `/home/jaret/repos/opencode-repos`. [score=0.839 recalls=14 avg=0.504 source=memory/2026-04-14.md:6-12]
<!-- openclaw-memory-promotion:memory:memory/2026-04-18.md:53:59 -->
- - Added a global backlog item, `G-033`, to define and implement the shared evergreen home for durable operating rules and capability playbooks that outlive individual projects. - Jaret wants backlog items written for fresh-agent handoff: plain, unambiguous titles, a concrete next action, and explicit references to the project notes or rule files needed to start cleanly without guesswork. # 2026-04-18 - Final OMS closeout comparison against the live files found no remaining implementation gap, only documentation drift. The durable state to remember is: the four shared OMS Phase 1 rules now truly live canonically in `40 Agent Nexus/Operating Rules and Playbooks/`, the OMS project-folder copies are historical-reference pointers, and OMS currently has no open local execution card. - The OMS project docs were reconciled to match reality: `Project Brief.md`, `Current State.md`, and `Phase 1 Action Plan.md` now reflect that Phase 1 is complete, the evergreen operating-rules folder is the shared canonical home, and future OMS work should reopen only when real shared-memory pain or a new promotion-worthy cross-project rule appears. - RI state correction from Jaret after a slightly stale heartbeat summary: `RI-028`, `RI-029`, and `RI-030` are done; `Research Brief Template.md` and `Research Run Summary Template.md` exist; `RI-031` is the current `Doing` card; and the RI `_index.md` has been updated to link the new templates. Treat this as the current no-blocker RI baseline. [score=0.839 recalls=14 avg=0.496 source=memory/2026-04-18.md:53-59]
<!-- openclaw-memory-promotion:memory:memory/2026-04-14.md:11:16 -->
- - Later on 2026-04-14, G-029 was completed and the earlier `gh auth` blocker was cleared. Durable state: `gh auth` is active on github.com as `johnny5-bot-isalive` with scopes `gist`, `read:org`, `repo`, and `workflow`. - The dedicated local repo home for OpenCode-managed work is now `/home/jaret/repos/opencode-repos`. - G-030 became the active OpenCode item. A baseline repo exists at `/home/jaret/repos/opencode-repos/opencode-test-repo`, and it has already been published to `https://github.com/johnny5-bot-isalive/opencode-test-repo`. - During G-030, the baseline repo was improved to be a reusable starter: `scripts/check-env.sh` was fixed so an empty `.env.example` does not fail under `set -euo pipefail`, a `Makefile` was added with reproducible `env-check`, `lint`, `test`, and `ci` targets, the `README.md` was expanded with baseline usage, and CI/test-glob behavior was tightened. `make env-check` and `make ci` now run successfully locally. - The Diffs plugin is now explicitly enabled in OpenClaw config. As a result, G-031 was tightened from "enable Diffs" to "verify an end-to-end diff-review flow now that Diffs is enabled." - Current OpenCode execution state at flush time: G-030 is still in Doing, the most recent local repo refinements are not yet pushed, and the recommended next move is to keep `opencode-test-repo` as the reusable baseline/template first, then create the first real app repo from that baseline. [score=0.838 recalls=10 avg=0.500 source=memory/2026-04-14.md:11-16]
